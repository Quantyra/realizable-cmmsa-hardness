# Actual readTableTag_of_rows proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `a59171452a27edff5eaf99365fbcf64e402a307f` (`prove actual readTableTag tree agreement`). Parent `1b47db71a587de7bb941f4d6d4acef6746cffb5c` (`Add certified GO/GO-WITH-NOTES Lean companion from formal-pvnp e8eb955`). Main SHA-256 `7F1866ED2F508B2909E9D90C323BA5C4259969829ED0214E7967C0B3536367DE`, Checks `278CD6A23C62792B75B62E670E1B44F256FCA45276FF6522CE005E94F465B621`. Independent `git cat-file` SHA-256 of both Lean blobs at `a591714` match the freeze and closeout (`blob 8ebb66f8f643bd3cd74c9c828c6d88aa728b33e9` / `8046170dcdd7f8804864d79ab479de48e5516a78`, 391914 / 5899 bytes, LF-only). Recomputed git-ids of those extracted blobs reproduce the same ids. Certification `f5995f2153c535675f71c137eb0b176fa522f7d7` (`certify actual readTableTag tree agreement (local after GCP auth fail)`) adds evidence only; Lean blobs unchanged from `a591714` (`git diff --name-only a591714 HEAD -- '*.lean'` empty). GCP `quantyra-lean-builder-01` unavailable (`Reauthentication failed`); local target-fresh of frozen blobs twice, main+Checks exit 0, objects `05288D3D0DDA3EFD6300642D10CE2774D4B77551DAFD010014D23880F97BEFE5` / `E615ED35D700F69C6C417E08391140702D4B6F0EDAD5B7B5B055A093D29F14E0`, `MATCH_MAIN=True MATCH_CHECKS=True`. Printed axioms of `readTableTag_of_rows` only `propext`, `Classical.choice`, `Quot.sound`; `readTableTag_empty` prints `propext` only; `gcdBits_mem_FP` / `readRatTag_mem_FP` / `_of_tree` / `readTableTag_mem_FP` / `listLenBits_mem_FP` / `decodeInputTag_empty` / `_none` / `_some` print the standard trio. `ActualSelectedCmmsaSeededMap.lean` untouched (`blob 9fa1a0ed`, same as `1b47db7`). Working-tree Main/Checks are later uncommitted Luna edits (`git diff --stat a591714` dirty on both Lean files); those edits are not part of this review. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze stop-loss: private `validRowsFlag_of_rows` agreeing with the three `ValidRows` conjuncts on packed `validRun` accumulators, then public `readTableTag_of_rows`, then stop. Public surface that compiled: `decodeInputTag` (def), `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits` / `_mem_FP` / `_odd_pair`, `readRatTag` / `_mem_FP` / `_of_tree`, `readSignedTag` / `_mem_FP` / `_of_tree`, `readListTag` / `_mem_FP` / `_of_tree`, `readFormulaTag` / `_mem_FP` / `_of_pair`, `readRowTag` / `_mem_FP` / `_of_pair`, `readRowListTag` / `_mem_FP` / `_of_pair`, `readParametersTag` / `_mem_FP` / `_of_tree`, `readTableTag` (def), `readTableTag_mem_FP`, `readTableTag_empty`, `readTableTag_of_rows`, `listLenBits` / `_mem_FP` / `_of_listTree` / `_leaf`. `decodeInputTag_mem_FP` is not declared as a public name; `selectedSeededMap` is not defined. Frozen `FiniteSourceSampler.ValidRows` is `0 < rows.length ∧ (∀ i : Fin rows.length, 0 ≤ (rows.get i).1) ∧ (∑ i, (rows.get i).1) = 1`. Public `readTableTag_of_rows` matches the freeze statement: packed `readTableTag` on `true :: encode (listTree (rows.map rowTree))` is that same tape if `ValidRows`, else `[]`. The private bridge is new: `foldAcc` accumulates `num/den` as `∑ (r.num.natAbs / r.den)` with `foldAcc_den_pos` / `foldAcc_ratio` / `foldAcc_eq_one_iff` under nonnegative rows; `validRows_iff` rewrites Fin-indexed `ValidRows` to membership plus `List.map` sum; `validClamp` is identity on well-formed prefixes by `foldAcc_bits_len` / `foldAcc_clamp` against `validBound = 2|enc|+64`; `validStep_walk_nonneg` consumes one encoded row per iterate via `mulCanon_eq_bits` / `addCanon_eq_bits` / `readDigitsTag_of_tree`; `validStep_walk_neg` sets `vFlag = [true]` on the first negative probability and stay-puts; `validRun_nonneg` / `validRun_neg` split the ruler `|enc|+1 ≥ rows.length` using `rowsEnc_length_ge`. Empty list never sets `vSeen`, so `validRowsSelect_seen_nil` yields `[]`. Nonempty nonnegative rows with `num.bits = den.bits` select `[true]`; unequal bits or a negative row select `[]`. `readTableTag_of_rows` is then `selectHead` of that flag: `[true]` keeps `true :: rowsEnc rows`, `[]` uses `selectHead_nil`. Checks `#check` / `#print axioms` the decodeInputTag names plus the packed gcd/rat/signed/list/formula/row/parameters/table/listLen surface including `readTableTag_of_rows`, inhabit `readTableTag ∈ FP` (and the prior packed tags), reduce `readTableTag [] = []`, reduce the empty-rows tree to `[]` from `readTableTag_of_rows`, and reduce `decodeInputTag []` / truncated `decodeInputTag [true] = []`.

Hunts (absent). `sorry` on tree agreement: no `sorry` / `admit` / `native_decide` / new `axiom` in main or Checks at `a591714`; the only `decide` uses in the new region are `0 < 2` / `2^n ≤ 2^max` Nat inequalities in `size_mul_le` / `size_add_le` (prior odd-part lemmas keep the same style). Claiming `decodeInputTag ∈ FP`: `decodeInputTag_mem_FP` is not a public name; `decodeInputTag_mem_FP_of_read` is still a private implication that needs `readInputTag ∈ FP`; Checks do not inhabit `decodeInputTag ∈ FP`; module and Checks headers say the name remains. Dummy identity: `id_mem_FP` is the Cobham stay-put / init API of `validStep` / `validLeaf` (and prior gcd/share/list stay-put); `constFn_mem_FP []` is the fail / empty-seen / unequal-sum branch of `validRowsFlag`; success of `readTableTag_of_rows` returning the same tagged tape is the freeze packing, not `fpSeededMap` of `id`, `identitySeededMap`, or a 3SAT→CMMSA compiler. Vacuous `readTableTag_of_rows`: the statement is `∀ rows`, including nonempty `ValidRows`-true tables; empty is the `0 < length` failure arm, not a restriction that makes the equality trivial. Clamp dropping bits: `vSucc_cons` rewrites `validClamp` to `take_of_length_le` under the `foldAcc_bits_bound` size proof, so well-formed `num.bits` / `den.bits` are not truncated before `eqFlag`. New axioms: none; printed set is the standard trio. Theorem 1 / `hSrcCmmsa`: not inhabited; `ActualSelectedCmmsaSeededMap.lean` is untouched.

Notes: Checks coverage of `readTableTag_of_rows` is the empty-list `[]` example only; there is no `rfl` on a concrete `ValidRows`-true table (single row of probability `1`, or a two-row split). Malformed packed-row tapes are fail-closed in the `readTableTag` definition (`emptyFlag` / `validRowsFlag` → `[]`) but are not a public theorem; `validRowsFlag_of_rows` is only on `rowsEnc`. `foldAcc` is introduced in this increment (parent had the packed walk without tree agreement). `maxHeartbeats` raises remain on prior gcd/quot proofs and are kernel budget, not a cheat. Main does not import `*Checks.lean`. Stopping after `readTableTag_of_rows` is the freeze-permitted stop-loss. Next consumer remains `readInputTag_mem_FP` then `decodeInputTag_mem_FP`; do not sorry those, and do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `decodeInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, P versus NP, publication.
