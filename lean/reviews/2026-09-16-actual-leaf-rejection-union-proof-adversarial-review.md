# Rejection union proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `37dae5120b9da738216a12845c0ed9e594d8f264`. Main `999A3183…33F83`, Checks `15F4924E…1AB86`. Certification `fa88d88`. Gate named `37dae51` not `bba6dbb`. 26 stages exit 0; main+Checks twice matching; objects `96C207B9…2C16` / `48EB9C81…6B8E`. Independent source hashes match. Independent rehash `result=PASS`, 175 rows 0 mismatches. Wrapper `CERT_EXIT=True` is PowerShell `$?` expansion, not a second compile. Forbidden scan clean. Printed axioms only `propext`, `Classical.choice`, `Quot.sound`.

Freeze public statements compiled. `restrictionAgreesOnCenter` is equality of transported versus queried restrictions on the queried center `Cw`; Rel and `transportedLeafLabel` are used. `restrictionAgreesOnCenter_iff_source` is `restrictToCenter_class_invariant`, not a tautology. Fail indicators are `if`-valued `ℝ` 0/1; `anyFailIndicator_le_sum` is `1 ≤` a nonnegative summand when some index fails, else `0 ≤ sum`; `anyFailIndicator_le_card` transits through `leafFailIndicator_le_one` and `card (Fin n) = n`. No PMF, measure, or uniform sampler. No tagged-failure import. Checks `#check` every public name, `#print axioms` every public theorem, and invoke `restrictionAgreesOnCenter_iff_source`, `anyFailIndicator_le_sum`, and `anyFailIndicator_le_card` on the freeze fixtures (identity Rel, distinct Rel pair, n=1 and n=2, empty `J=h=0` at n=0 and n=1).

Notes: `Cv` and `hK` are unused in the `restrictionAgreesOnCenter` body; the module silences `linter.unusedVariables`. `hK` is load-bearing in `iff_source` via class invariance. Nonempty fixtures have `L=⊥` and `K=⊥`, so `Hom(⊥, ZMod 2)` is unique and every fail indicator is 0; `midPackaged` is defined as the transport of `sourcePackaged`, so the distinct-Rel pair agrees by construction. `leafFailIndicator_nonneg` / `le_one` are `#check`ed and used inside `le_sum` / `le_card`, not applied as standalone examples. Empty Check is a vertex against itself. Not credited: star acceptance, hardness, Theorem 1, Corollary 2, P-versus-NP, publication.
