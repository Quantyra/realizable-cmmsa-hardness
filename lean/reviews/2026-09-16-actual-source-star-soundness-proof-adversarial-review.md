# Actual source-star soundness proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `f35269d0abecc290f2d79e25a2b1a470d738dc43`. Main `0C42DE92…6F9B`, Checks `2870AAA5…254D`. Certification `ce9ac48`. Gate named `f35269d` not `bba6dbb`. 42 stages exit 0 including `RejectionUnionChecks`, `actual-star-acceptance`, and `actual-source-star-completeness`; main+Checks twice matching; objects `AD8FFF6C…2527` / `1016B717…0CA2`. Independent source hashes match. Independent rehash `result=PASS`, 271 rows 0 mismatches. Cert commit adds evidence only; Lean sources unchanged from `f35269d`. Forbidden scan clean. Printed axioms only `propext`, `Classical.choice`, `Quot.sound`.

Freeze public statements compiled. `questionFailIndicator` is the existential of `ofActual.badRow` on `P.U` as a 0/1 real; `eq_zero_iff` / `eq_one_iff` transport that through certified `honestRawLabel_respects_iff` and a 0-or-1 split, not a tautology and not a decoder. `presentedEnum` is `P.U.equivFin.symm` along `P.card_U`; `mem` and `surj` give image `P.U`, so the `Fin J` exists matches `actualBaseFailureIndicator`. `le_sum` is the same 1-versus-positive-summand argument as `actualBaseFailureIndicator_le_sum`, indexed over `RowId`. `questionFail_sourceExtension_original` requires `hU` (every queried row is `Sum.inl`) and rewrites through `ofActual_badRow` then certified `sourceExtension_original_bad`; original-row identification is not skipped. `anyQuestionFailIndicator_le_sum` is the family union bound. Comment cites `actualTaggedGoodFailureMean_sourceExtension_le` as next consumer only; this module does not re-prove or state a `(4/3)*J*eta` mean. No Rel packaging. Checks do not import `*Checks.lean`. Checks `#check` every public name, `#print axioms` every public theorem, and invoke `eq_zero_iff`, `eq_one_iff`, `eq_baseFailure`, `le_sum`, `presentedEnum_mem`/`surj`, `questionFail_sourceExtension_original` on an original-row presentation, and `anyQuestionFailIndicator_le_sum` at n=1 and n=2. Nonempty mixed-RHS three-row fixture matches completeness: completeness `x` gives fail=0; all-zero violating original row 0 gives fail=1. Empty `J=h=0` is `U=∅` and fail=0 for every `x`.

Notes: this increment is the leaf-side 0/1 question-fail indicator, its coercion to the certified base block-failure indicator, original-row source-extension identification, and the 0/1 union bound. It does not prove that a failing question prevents star acceptance or packaging of an honest label. `presentedEnum` injectivity is not a public theorem (exists-equivalence does not need it). Source-extension Check invokes the identification and does not reduce the concrete `if` to 0 or 1. `anyQuestionFail` Checks invoke the inequality only, including n=2 where only original row 0 is violated. Fixtures use `L=⊥`. Module silences `linter.unusedVariables`. Not credited: decoder, `(4/3)*J*eta` mean, NP-hardness, Theorem 1, Corollary 2, SeededMap, P-versus-NP, publication.
