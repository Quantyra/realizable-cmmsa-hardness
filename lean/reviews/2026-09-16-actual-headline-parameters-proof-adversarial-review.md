# Actual headline σ_L / γ_L parameters proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `605b07e8d192f13772bdeac9ab16e3772b491d5a` (`prove actual headline sigma_L gamma_L parameters`). Main `29767BDCE518085DB0590A260293003083858169FCC30D49A299217D913FC20C`, Checks `1000E3BD838BF74C7C3EE923F5816E45421169C471FBC7BE49C3A556EE949B48`. Certification `8ce8e2ace80fb2a7824a506cb2a67ead4790f6f4`. Gate named `605b07e` not `bba6dbb` (`certify.sh` rejects the sibling hash; `gate.sh` pins `EXPECTED_HEAD=605b07e…`). 11 stages exit 0 (`exception-repair`, `formula`, `cmmsa-codec`, `randomized-reduction`, `randomized-reduction-assembly`, `actual-cmmsa-randomized-reduction`, `actual-theorem1`, main+Checks twice); objects `D0631689367748FD62E28CDCC084353E51B225195CDE43D0612B9D00CEA6A7B1` / `7353C5A3D474CC0C920941DA9DE3A0D3249AFC5FD810DFA43828BD41D24006E2`. Independent working-tree SHA-256 of both Lean sources match the freeze and the gate. Cert commit adds evidence only; Lean blobs unchanged from `605b07e`. Independent rehash `result=PASS`, 85 rows 0 mismatches. Forbidden scan clean. Printed axioms only `propext`, `Classical.choice`, `Quot.sound` (`mOf_spec` axiom-free). I made no Lean-source or Git changes and ran no additional broad build.

Freeze public statements compiled. `mOf` is `0` when `sqrt(log2 L) < 256`, else that square root: largest admissible `m`. `mOf_spec` is that disjunction, not `∨ True`. `mOf_maximal` is maximality among `256 ≤ m ≤ sqrt(log2 L)`. `mOf_unbounded` witnesses `L0 = 2^{(max(M,256))^2+1}` so `m(L) → ∞`. `hOf` is `b_m * floor(log2((L-1)/(q(m+1))) / (2 b_m))` with `b_m = max(m,1)` and `den` guarded by `max den 1`. `ROf = 2^(2h)`, `GammaOf = 2*(3/4)^q`, `sigmaL = (R/4)/2`, `gammaL = 2*GammaOf`. `hOf_le_log2` is `2 b_m floor(log2(num)/(2 b_m)) ≤ log2(num) ≤ log2 L` via `Nat.mul_div_le` and `num ≤ L`. `sigmaL_le_ROf` is the two Nat floors. The freeze’s `log_sigmaL_nearlinear_eventual` template with `∨ True` was dropped, not shipped. `sigmaL_ge_one_eventual` is not vacuous: from `mOf_unbounded 256` it gets `s ≥ 256`, `2^(s^2) ≤ L`, `q(s+1)2^(4s)+1 ≤ 2^(s^2)`, `num ≥ 2^(4s)`, `log2(num) ≥ 4s`, `h ≥ 2`, `R ≥ 8`, hence `floor(floor(R/4)/2) ≥ 1`. `gammaL_pos` holds for every `L`. `gammaL_pos_lt_one_eventual` uses `mOf_unbounded 36` so `q ≥ 6` and `4*(3/4)^6 < 1`. `gammaL_small_eventual` is a real `γ_L → 0`: pick `k` with `(3/4)^k < ε/4` and take `m ≥ k^2`. `adviceLeaf` / `sigmaLearn` / `gammaLearn` are definitions (`Nat.sub`, `(49 * sigmaL _)/100`, `5 * gammaL`). `theorem1_headline` is `theorem1_realizable_cmmsa` at `sigmaL` / `gammaL`; `hσ` / `hγ0` / `hγ1` and the two `Preserves (1/6)` maps remain hypotheses.

Hunts (absent). `∨ True`: no public or private disjunct of that form; `mOf_spec`’s `= 0 ∨ …` is the freeze’s “none” case. Dummy SAT-to-CMMSA / identity as `hSrcCmmsa`: not present in main or Checks; `theorem1_headline` forwards the two `∃ Preserves (1/6)` binders into `theorem1_realizable_cmmsa` and does not construct a `SeededMap`. Claiming `log(sigma_L)/log L → 1` without a real inequality: no such theorem; the freeze-preferred honest pair `hOf_le_log2` and `sigmaL_le_ROf` is what compiled. Inhabiting `theorem1_headline`: Checks `#check` / `#print axioms` only; the `example`s are `mOf 0 = 0`, `sigmaL 0 = 0`, `gammaL 0 = 4`, and the two honest inequalities at `L = 0` and `L = 2^20`. `#eval` at `2^20` is a real reduction (`mOf = 0`, `sigmaL = 32768`, `gammaL = 4`), not a dummy constant family. No `sorry` / `admit` / `native_decide` / new `axiom`.

Notes: the matching manuscript lower bound `2h ≥ log2 L - O(log(m+1)+m)` is not a public theorem, so `hOf_le_log2` + `mOf_unbounded` must not be read as `log σ_L / log L → 1`. Eventual `L0` is explicit but astronomical (`2^(256^2+1)`); `L = 2^20` is outside the live range (`sqrt(20) < 256`, `gammaL = 4 ∉ (0,1)`). `hOf` totalizes with `Nat.max den 1` and `Nat.max m 1`; those maxima are identities once `m ≥ 256`. `theorem1_headline` does not quantify “sufficiently large `L`” internally and does not inline the eventual lemmas. `adviceLeaf` uses floor-log rather than ceil (freeze-specified) and has no eventual `1 ≤ sigmaLearn` / `gammaLearn < 1` lemma. `hOf_le_log2` prints classical axioms on a Nat-only statement (Mathlib/`Nat.log`/module imports, not a new axiom). Module does not import `*Checks.lean`.

No HIGH vacuity, hypothesis, or statement bug. Not credited: unconditional Theorem 1, inhabited `RandomizedPromiseNPHard (cmmsaPromise L (sigmaL L) (gammaL L) …)` without the two `1/6` maps, SAT-to-source or source-to-CMMSA `Preserves (1/6)` constructions, `log(sigma_L)/log L → 1`, Corollary 2 NP-hardness, P versus NP, publication.
