# Actual CMMSA randomized-reduction interface proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `c0583486d30e65fa3c672a21b163be6f5c1c5b0e` (`prove actual CMMSA randomized-reduction interface`). Main `9CDDC139FCF64A07A4E759AD8F5AA78337123FB0EA131E77924421FC4F776283`, Checks `BF1C7E8BAA430A75CDC94F6E5DE8A68406000C8E23CE0D4955B18546B7390850`. Certification `c01f6e3bc714ec90a0c00ac4530d4ebdb98bcddd`. Gate named `c058348` not `bba6dbb`. 9 stages exit 0 (`exception-repair`, `formula`, `cmmsa-codec`, `randomized-reduction`, `randomized-reduction-assembly`, main+Checks twice); objects `427BFF0D786038838A14FC03F05C44FC0CA499ADED714D48E55AE9DD5BAA42B3` / `A5B5B80CC8D47625A6DAB4B0A8F7563460EF0B41E2994B8B06A95C331F60117F`. Independent working-tree SHA-256 of both Lean sources match the freeze and the gate. Cert commit adds evidence only; Lean blobs unchanged from `c058348`. Independent rehash `result=PASS`, 73 rows 0 mismatches. Forbidden scan clean. Printed axioms only `propext`, `Classical.choice`, `Quot.sound`. I made no Lean-source or Git changes and ran no additional broad build.

Freeze public statements compiled. `cmmsaPromise` YES is `decode` plus `CMMSACodec.Yes 0`; NO is `decode` plus `CMMSACodec.No sig gam`. `disjoint` is a proved structure field, not a stored hypothesis: `Option.some.inj` identifies the two decoded instances; the `Yes 0` witness has `cost ≤ budget` and `satisfaction ≥ 1`; `1 ≤ sig` scales that cost into the `No` quantifier; `hN` then gives `satisfaction < gam < 1`. `cmmsaPromise_yes_of_encode` / `no_of_encode` are `decode_encode` plus the given `Yes`/`No` hypothesis. `RandomizedMapReduces` is `∃ R, Preserves R source target (1/3) (1/3)`. `RandomizedPromiseNPHard` is the predicate `∀ A ∈ NP, RandomizedMapReduces (ofLanguage A) target`; it is defined and `#check`ed, not instantiated on `cmmsaPromise`. `two_stage_seededMap` is `exists_preserving_composition` at `1/6+1/6=1/3` with polynomial coins and the original-input-length TM clock retained; `convert` rewrites the summed errors by `norm_num`, and does not hide the clock. `two_stage_randomized_map` packages that `C` as `RandomizedMapReduces` and drops the coin/clock witnesses, matching its freeze signature. `identitySeededMap` is zero-coin `pairFst` with `constFn []` ruler; `identitySeededMap_apply` / `_success` / `_preserves` are the identity self-map at error `0`, not a SAT-to-CMMSA map. `preserves_mono` is `1-e' ≤ 1-e ≤ success` from `e ≤ e'`; the extra unused `e' ≤ 1` binders are not used in the proof. Checks `#check` every public name, `#print axioms` every public theorem, copy the codec `concrete : Instance 1`, show `decode 1 [] = none` is neither YES nor NO, run `identitySeededMap_preserves` on `cmmsaPromise 1 1 (1/2)`, and instantiate `two_stage_randomized_map` on identity after `preserves_mono` lifts `0` to `1/6`.

Notes: `disjoint` invokes `0 < i.data.budget` from `Instance.valid` only to feed `le_of_lt` into `le_mul_of_one_le_left`, which needs `0 ≤ budget`. Strict budget positivity is stronger than the freeze argument (`cost ≤ budget` and `1 ≤ sig` send the witness into `No`). `hgam : 0 < gam` is unused (`have _ := hgam`); the contradiction uses `gam < 1`. This is not a soundness gap on the certified `Valid` carrier. Source comments call `identitySeededMap` a runtime/asymptotics baseline and deny SAT-to-source / source-to-CMMSA `Preserves`; Checks `RandomizedMapReduces samplePromise samplePromise` is identity composed with itself, not a SAT-to-CMMSA reduction. Checks never apply `cmmsaPromise_yes_of_encode` / `no_of_encode`, never inhabit `Yes 0` or `No` on `concrete` (the copied fixture is a perfect-satisfaction instance: one weight `1`, duplicated `var 0`, budget `1`), and do not name-check `disjoint`. Coverage of the promise content is empty-string and identity only; the freeze's "if easy" Yes/No exhibition was skipped. `preserves_mono`'s unused `e' ≤ 1` hypotheses are extra. Module does not import `*Checks.lean`.

No HIGH vacuity, hypothesis, or statement bug. Not credited: Theorem 1, Corollary 2, `RandomizedPromiseNPHard (cmmsaPromise ...)`, SAT-to-source or source-to-CMMSA `Preserves`, P versus NP, publication.
