# Actual selected CMMSA SeededMap surface proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `38d9ad0df63e42cea9190657f836822892184010` (`prove actual selected CMMSA SeededMap surface`). Main `A0EFD901492C1D0DDB0012B3CDF545B06C51574812F5AC894257309E0B560786`, Checks `3F4971152605229124AB5D1EF04D6758FF89589F8B42F4E843CC2138D5A5136C`. Certification `1d18f76dcf4c131c4d86c6737a1e045403468ff8`. Gate named `38d9ad0` not `bba6dbb` (`certify.sh` rejects the sibling hash `bba6dbb380fa5dde490c45fd7806ccb05aa1e8a8`; `gate.sh` pins `EXPECTED_HEAD=38d9ad0…`). 32 stages exit 0 (`exception-repair` through `actual-sat-to-threesat-source`, then main+Checks twice); objects `1CC96DB95ADB16F2F2A873C4C5A7A92347EB9A86A6512488438092F809F7F3B3` / `E3C0104574AC4D9588721DC5D66595522F04EBDD848A27954E2A7E1D71F3BDE8`. Independent working-tree SHA-256 of both Lean sources match the freeze and the gate. Cert commit adds evidence only; Lean blobs unchanged from `38d9ad0`. Independent rehash `result=PASS`, 211 rows 0 mismatches. Forbidden scan clean. Printed axioms only `propext`, `Classical.choice`, `Quot.sound`. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze fallback, not the full SeededMap package. Public surface that compiled: `selectedPairedRun` (def), `selectedCoinRuler` (def), `selectedCoinRuler_mem_FP`. `selectedPairedRun_mem_FP` is omitted; `selectedSeededMap` is not defined; `selectedPipelinePromise` and `threeSat_to_cmmsa_of_compiler` are absent. `selectedCoinRuler` is `List.replicate (coinRuler eps x.length) false`. `selectedCoinRuler_mem_FP` is Cobham length arithmetic, not a parser: `mulLenFn_mem_FP` of `id` with itself (`n*n`), `mulLenFn_mem_FP` of `const_replicate_mem_FP 11` with `id` (`11*n`), `appendFn_mem_FP` of those two (`n^2+11n`), then `mulLenFn_mem_FP` of `const_replicate_mem_FP (512 * inverseCeil eps ^ 3)` against that quadratic. Equality to `coinRuler` is `coinRuler_quadratic` plus `Nat.pow_two`: `(64*(n+11)*inv^2)*(8*n*inv) = 512*inv^3*(n^2+11n)`. The `id` wrappers are the Cobham API (`mulLenFn` takes `FP` functions), not a 3SAT identity. `selectedPairedRun` is `paddedRun L eps (pairFst z) (pairSnd z)`; `paddedRun` is already total (`paddedRunOption.getD []`). Checks `#check` the three public names, `#print axioms` of `selectedCoinRuler_mem_FP`, prove ruler length equals `coinRuler`, and show `selectedPairedRun L (1/4) [] = []` by `pairFst [] = []`, `pairSnd [] = []` (`unpair? [] = none`), and `decodeInput_empty`.

Hunts (absent). `sorry` on `run_fp`: no `sorry` / `admit` / `native_decide` / new `axiom`; `selectedPairedRun_mem_FP` is not declared, so `SeededMap.run_fp` cannot be filled. Dummy 3SAT→CMMSA: no `identitySeededMap`, no `MapReducesVia`, no `Preserves`, no compiler, no `fpSeededMap` of `id`. Claiming `selectedSeededMap` exists: no `def selectedSeededMap`; both module headers state it is not defined because `paddedRun` / `decodeInput` / `Tree.parse` are not on the Cobham surface. `hSrcCmmsa` is not inhabited; `theorem1_from_threeSat_to_cmmsa` is imported via `ActualSatToThreeSatSource` and unused.

Notes: commit and module names say “SeededMap surface” while the Lean object is a ruler plus an unproved executor; the docstrings are the honest boundary. `open RandomizedReduction` / `ActualHeadlineParameters` / `ActualSatToThreeSatSource` are unused leftovers from the intended full increment. `selectedCoinRuler_mem_FP` is for every `Rat` `eps`, including non-positive (where `inverseCeil` degenerates); that is FP membership of a length-only function, not sampling correctness. Checks coverage of `selectedPairedRun` is the empty string only. Main does not import `*Checks.lean`. Next consumer remains Cobham `run_fp` for `paddedRun` on all inputs, then `selectedSeededMap`, then a 3SAT→`encodeInput` compiler; do not sorry `run_fp` and do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `selectedSeededMap`, `selectedPairedRun_mem_FP`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, P versus NP, publication.
