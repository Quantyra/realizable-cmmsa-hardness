# Actual packed gcdBits FP tag: non-claims-boundary review

**GO-WITH-NOTES.**

Frozen source `7105c9504c55226b4143624a4f0143379e11c1ba`. Gate logs name `7105c95`. Main `ActualDecodeInputFP.lean` SHA-256 `8C9B46562B478EB93870249071E8636FFEC80A6F8A068E681D2467E0963FBD8A` (git blob `7fff1264a3d6de0eb5bb5857ea4f638222992a27`, 75230 bytes). Checks SHA-256 `3FF960978E4C1567E74B764704612B72044A76639DA2D18EFAC0EF92921422E6` (git blob `bf6d15592bbe352aadd5f94971dc322bf699baa5`, 1108 bytes). Evidence `research/evidence/2026-09-16-actual-gcd-bits-fp-fresh-run/`. Independently rehashed local sources at `7105c95`, certify commit `7ccc144`, `HEAD`, and the working tree (Checks byte-identical; Main identical after CRLF→LF) match the freeze and the gate. Certification commit `7ccc144` did not change the Lean sources. Independent rehash of the certification artifact table: PASS, 175 rows, 0 mismatches; `artifact-hashes.txt` SHA-256 `6443BE05228C45699589EC8E02667535E321447915823CC9197636EE37FA3786`. Recorded objects `68D4E315CDC58A7410C0A5BB1C07ED5675F268040A26121BC735F24C52B749AD` / `4B90D9D4541098CE91846ECD6BA6945FB57A3122F28CF9F106593D01473315C8`. Closeout SHA-256 `2304EFD6FDABD1F794BCF326393B84CFA8C9C7618051A6C7639611AC51918DF4`.

Accepted, freeze stop-loss only (`gcdBits ∈ Complexity.FP`; total packed iterate of `gcdStep`; no `readRat ∈ FP`, no `decodeInput ∈ FP`):

- `gcdBits z` is `pairFst (gcdStep^[(gcdRuler z).length] z)`, a total `List Bool → List Bool` packing of the subtractive/`dropTwos` binary-GCD step on a pair of digit tapes. `gcdBits_mem_FP` places that packed function in `Complexity.FP` from Cobham `iterate_mem_FP` of `gcdStep` with polynomial `gcdRuler` / `gcdWidth`, then `fstBlock`. It is not a semantic identity `gcdBits = encode ∘ gcd`, not `GCD ∈ FP` as a number-theoretic map, and not `readRatTag ∈ FP`.
- `subCanon_bitValue` is a private length/value lemma used to close the iterate width. It is not a public theorem and is not an FP membership.
- Public packing of `decodeInput` exists as freeze-permitted progress: `decodeInputTag` / `_empty` / `_none` / `_some`. There is no `decodeInputTag_mem_FP`, no `decodeInput ∈ FP`, and no Checks example inhabiting `decodeInputTag ∈ Complexity.FP`.
- `readRatTag` is not defined. There is no `readRatTag_mem_FP` and no `readRat ∈ FP`. `readInputTag` is private; `decodeInputTag_mem_FP_of_read` is a private conditional (`readInputTag ∈ FP → decodeInputTag ∈ FP`), not an unconditional membership.
- `selectedSeededMap` is not defined. `hSrcCmmsa` is not inhabited. There is no dummy 3SAT→CMMSA identity.

The module header denies `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, and P vs NP. Checks `#check` / `#print axioms` `decodeInputTag`, `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits`, and `gcdBits_mem_FP`; inhabit only `gcdBits ∈ Complexity.FP`; and reduce `decodeInputTag [] = []` and truncated `decodeInputTag [true] = []`. Checks do **not** `#check` `readRatTag_mem_FP`, `decodeInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, or `hSrcCmmsa`. Main does not import `*Checks.lean`. `#print axioms gcdBits_mem_FP` is `propext`, `Classical.choice`, `Quot.sound` only. Forbidden-scan is clean. README, `INTEGRITY-CLAIMS.md`, `CHANGELOG.md`, and `CITATION.cff` are unchanged by this increment.

Notes, not blocking:

- The Main module docstring still says packed `ExecutablePipelineInput.decodeInput` is a total `List Bool → List Bool` function in `FP`. That is the file-goal catalog line, not a theorem. Do not cite the docstring as `decodeInput ∈ FP` or `decodeInputTag ∈ FP`. The Checks header and the closeout keep `readRatTag_mem_FP` / `decodeInputTag_mem_FP` open.
- Module/commit titles that say "actual gcdBits packed FP tag" / `ActualDecodeInputFP` are catalog labels. They name `gcdBits ∈ Complexity.FP`, not the un-packed semantic GCD, not `readRat ∈ FP`, not `decodeInput ∈ FP`, not `selectedSeededMap`, not inhabited `hSrcCmmsa`, not unconditional Theorem 1, Corollary 2, publication, or `P` versus `NP`.
- Private `readDigitsTag_mem_FP` / `readNatTag_mem_FP` / `quotBits_mem_FP` / `dropTwos_mem_FP` / `gcdStep_mem_FP` are supporting Cobham lemmas. They are not additional public claims.
- The truncated-`true` check is a runtime packing identity `decodeInputTag [true] = []`. It is not a named public FP theorem and is not `decodeInput ∈ FP`.
- Closeout phrasing "packed binary GCD in FP" is the packing `gcdBits`. Do not cite it as semantic GCD correctness or as `readRat ∈ FP`. The next freeze's semantic-GCD / `ratTree` obligation remains open.
- Stopping after `gcdBits_mem_FP` is the freeze-permitted stop-loss. It does not expand the claim boundary to `readRatTag`, `decodeInputTag`, or the rest of the SeededMap route.

Forbidden claims absent. The freeze’s `readRatTag_mem_FP` / `decodeInputTag_mem_FP` / `selectedPairedRun_mem_FP` / `selectedSeededMap` / `hSrcCmmsa` target remains open. Next: `readRatTag_mem_FP` (semantic GCD of the iterate, then reduced `ratTree`), then `decodeInputTag_mem_FP`. Then `selectedPairedRun_mem_FP` and `selectedSeededMap`. Then a 3SAT (or 3-Lin) → `encodeInput` table compiler ∈ FP with `MapReducesVia` into a selected pipeline promise. Do not inhabit `hSrcCmmsa` with identity, do not sorry `readRatTag_mem_FP` or `decodeInputTag_mem_FP`, and do not skip to Theorem 1.
