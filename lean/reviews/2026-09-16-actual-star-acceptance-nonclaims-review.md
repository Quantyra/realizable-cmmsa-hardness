# Star acceptance non-claims-boundary review

**GO-WITH-NOTES.**

Frozen `5606cd9`. Gate logs name `5606cd9`, not `bba6dbb`. Accepted: a queried `Fin n` tuple `starAccepts` iff every transported restriction agrees with the queried restriction on hypothesized shared `K` iff `anyFailIndicator = 0`. Constant-center `starAcceptsCenter` is packaging of constant `vs`, equivalent by `Iff.rfl`. PMF omitted. Module/commit titles that say "star acceptance" and the doc comment "the star test accepts" are catalog labels, not `StarListDecoding.Star.accepts`, source-to-star soundness, or a CSP test. `starAccepts_of_source_agrees` is pointwise `restrictionAgreesOnCenter_iff_source`, not outer/source hardness. Forbidden claims absent. Manuscript theorem incomplete. Next: outer/source hardness.
