# Actual decodeInput packed FP tag proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `f7f338bdd28101ff66fa482716a435ca783cd191` (`prove actual decodeInput packed FP tag`). Parent `98ffac04e6e9ce96db5690e47aa3143c927ef568` (`review actual readTableTag tree agreement`). Main SHA-256 `D2405DC2CE759190671801AEF669297C1F8DD9F56BAC1079F7E95D9EB87DDD87`, Checks `BAD35DD0BFFD06A007E5C552C494AAA823AF1B266CB2F74498EF01E0B614DC9D`. Independent `git cat-file` SHA-256 of both Lean blobs at `f7f338b` match the freeze and closeout (`blob aba9e488124928fba7d18fbb22f6d056b20c8eff` / `aef903bfaf40998fb168184274973214f62bcee4`, 402609 / 6581 bytes, LF-only). Recomputed git-ids of those extracted blobs, and `git hash-object` of the same bytes, reproduce the same ids. Certification `29a8713019c550075e72e821a7570a7f7efc1b02` (`certify actual decodeInput packed FP tag (local after GCP auth fail)`) adds evidence only; Lean blobs unchanged from `f7f338b` (`git diff --name-only f7f338b 29a8713 -- '*.lean'` empty; same blobs at `HEAD`). GCP `quantyra-lean-builder-01` unavailable (`Reauthentication failed`); local target-fresh of frozen blobs twice, main+Checks exit 0, objects `773E14769F846688877E9E0857873E8AB3286E4CBA43D3D3161D6A717ECBB8E8` / `BB977DD6D036454A4C9B5978347A5D345D62F3CA0B816030172A55B57DDF3BC2`, `MATCH_MAIN=True MATCH_CHECKS=True`. Printed axioms of `decodeInputTag_mem_FP`, `readInputTag_mem_FP`, and `readInputTag_of_tree` only `propext`, `Classical.choice`, `Quot.sound`; `gcdBits_mem_FP` / `readRatTag_mem_FP` / `_of_tree` / `readTableTag_mem_FP` / `readTableTag_of_rows` / `listLenBits_mem_FP` / `decodeInputTag_empty` / `_none` / `_some` print the standard trio; `readTableTag_empty` prints `propext` only. `ActualSelectedCmmsaSeededMap.lean` untouched (`blob 9fa1a0ed2649fcb3424c62a861abd545f24b0357`, same as `98ffac0` / `1b47db7`). Working tree is clean versus `HEAD` / `f7f338b` on `*.lean`. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze stop-loss: replace the parent private semantic stub `readInputTag` (parse then `ExecutablePipelineInput.readInput`) with a packed Cobham walk of the five-tuple `inputTree`, prove `readInputTag_mem_FP` and `readInputTag_of_tree`, then close `decodeInputTag_mem_FP` by the existing private `decodeInputTag_mem_FP_of_read`, then stop. Public surface that compiled: `decodeInputTag` (def), `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits` / `_mem_FP` / `_odd_pair`, `readRatTag` / `_mem_FP` / `_of_tree`, `readSignedTag` / `_mem_FP` / `_of_tree`, `readListTag` / `_mem_FP` / `_of_tree`, `readFormulaTag` / `_mem_FP` / `_of_pair`, `readRowTag` / `_mem_FP` / `_of_pair`, `readRowListTag` / `_mem_FP` / `_of_pair`, `readParametersTag` / `_mem_FP` / `_of_tree`, `readTableTag` / `_mem_FP` / `_empty` / `_of_rows`, `listLenBits` / `_mem_FP` / `_of_listTree` / `_leaf`, `readInputTag` (def), `readInputTag_mem_FP`, `readInputTag_of_tree`, `decodeInputTag_mem_FP`. `selectedSeededMap` is not defined. Parent `98ffac0` left `decodeInputTag_mem_FP_of_read` as a private implication that needs `readInputTag ∈ FP`, with `readInputTag` still a private `Tree.parse` / `readInput` pack (not a Cobham machine). This commit deletes that stub, exposes packed `readInputTag`, and inhabits the implication. Packed walk: `splitNode` four times on `node ws (node rows (node q (node b m)))`; `inputWTag = readListTag ∘ nodeLeft`; `inputRArg` pairs `listLenBits (dropOne inputWTag)` with the rows tree (`readRow weights.length`); `inputRTag = readRowListTag ∘ inputRArg`; `inputTTag = readTableTag ∘ inputRTag`; `inputPTag = readParametersTag` on `q`; `inputBTag` / `inputMTag = readNatTag` on precision / trials; fail-closed `selectHead (emptyFlag ·) []` on each field; success reassembles `wrapInputEnc` = `true :: encodeInput x` by `wrapInputEnc_eq`. `readInputTag_mem_FP` is nested `selectHeadFn_mem_FP` / `emptyFlagFn_mem_FP` of those already-certified packed tags. `readInputTag_of_tree` is `∀ t`, matching `readInput`: malformed spines `[]`; `readList readSigned` fail `[]`; `readList (readRow weights.length)` fail via `readTableTag_empty`; `ValidRows` false via `readTableTag_of_rows` / `if_neg`; `readParameters` / `readNat` fail `[]`; all-some and `ValidRows` true via `if_pos` then `wrapInputEnc_eq ⟨weights, ⟨rowlist, hval⟩, params, prec, trials⟩`. `decodeInputTag` remains the pack `none ↦ []` / `some x ↦ true :: encodeInput x`. `decodeInputTag_eq_guard` rewrites it to `treeParseTag` then rest-empty `selectHead` then `readInputTag (encode t)`, using `readInputTag_of_tree` on the empty-rest arm (trailing bits stay `[]`, matching `decodeInput`). `decodeInputTag_mem_FP` is `decodeInputTag_mem_FP_of_read readInputTag_mem_FP` via `mem_FP_of_eq` of that composition. Checks `#check` / `#print axioms` the prior packed surface plus `readInputTag` / `_mem_FP` / `_of_tree` and `decodeInputTag_mem_FP`, inhabit `readInputTag ∈ FP` and `decodeInputTag ∈ FP`, and reduce `readInputTag (encode leaf) = []` from `readInputTag_of_tree`.

Hunts (absent). `sorry` on packed decodeInput FP: no `sorry` / `admit` / `native_decide` / new `axiom` in main or Checks at `f7f338b`; the only `decide` uses remain prior `Nat.div_lt_self` fuel and `0 < 2` / `2^n ≤ 2^max` inequalities in odd-part / table-size lemmas. Claiming un-packed `decodeInput ∈ FP`: the public theorem is `decodeInputTag ∈ Complexity.FP`, the packed `List Bool → List Bool` tag, not `decodeInput : Bits → Option Input`; Checks inhabit `decodeInputTag ∈ Complexity.FP` and do not inhabit a semantic `decodeInput ∈ FP`. Dummy identity: `constFn_mem_FP []` is the fail / empty-flag arm of `readInputTag` / `decodeInputTag_eq_guard`; `id_mem_FP` is not used in the new region; `wrapInputEnc` is the `inputTree` concatenation, not `fpSeededMap` of `id`, `identitySeededMap`, or a 3SAT→CMMSA compiler. Vacuous `readInputTag_of_tree`: the statement is `∀ t`, including the nonempty `ValidRows`-true five-tuple success arm that returns `true :: encodeInput x`; leaf / truncated spines are fail arms, not a restriction that makes the equality trivial. Checks not driving the real theorems: Checks `#check` / `#print axioms` / inhabit `readInputTag_mem_FP` and `decodeInputTag_mem_FP`. New axioms: none; printed set is the standard trio. Theorem 1 / `hSrcCmmsa`: not inhabited; `ActualSelectedCmmsaSeededMap.lean` is untouched.

Notes: Checks coverage of `readInputTag_of_tree` is the leaf `[]` example only; there is no `rfl` on a concrete well-formed `Input` (nonempty `ValidRows` table plus weights / parameters / precision / trials). Malformed non-tree tapes are fail-closed in the packed `selectHead` walk but are not a public theorem; `readInputTag_of_tree` is only on `encode t`. `decodeInputTag` is still defined by matching semantic `decodeInput`, then transferred into `FP` by `mem_FP_of_eq` against the `treeParseTag` / `readInputTag` machine; that is the freeze packing, not a proof that the `Option Input` decoder is itself a Cobham function. Parent semantic stub `readInputTag` is gone. `maxHeartbeats 4000000` on `readInputTag_of_tree` is kernel budget, not a cheat. Main does not import `*Checks.lean`. Stopping after `decodeInputTag_mem_FP` is the freeze-permitted stop-loss. Next consumer remains `selectedPairedRun_mem_FP` then `selectedSeededMap`; do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, dummy 3SAT→CMMSA identity, unconditional Theorem 1, Corollary 2, P versus NP, un-packed `decodeInput ∈ FP`, publication.
