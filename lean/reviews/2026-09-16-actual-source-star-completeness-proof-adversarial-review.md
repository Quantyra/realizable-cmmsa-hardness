# Actual source-star completeness proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `c574bd9549543368f26902447a33c7c3427fef3e`. Main `192ABD6E…E648`, Checks `2C8E0B3D…097D`. Certification `5b245fe`. Gate named `c574bd9` not `bba6dbb`. 29 stages exit 0 including `RejectionUnionChecks` and `actual-star-acceptance`; main+Checks twice matching; objects `0E22937A…68D3` / `CA88A2BA…06F9`. Independent source hashes match. Independent rehash `result=PASS`, 193 rows 0 mismatches. Forbidden scan clean. Printed axioms only `propext`, `Classical.choice`, `Quot.sound`. Cert commit adds evidence only; Lean sources unchanged from `c574bd9`.

Freeze public statements compiled. `honestRawLabel` is the ambient pairing `∑ z_i x_i` on an arbitrary domain, with additivity and homogeneity. `honestRawLabel_eval_equation` rewrites that pairing on `equationVector e` by `support_eq`, `row_injective`, and `Fin.sum_univ_three` to `x(row e 0)+x(row e 1)+x(row e 2)`; not a tautology. `honestRawLabel_respects_iff` is that pairing-equals-RHS on `P.U` iff every queried `ofActual.badRow` is false, via classical DNE on `decide`; both directions use `eval_equation`. `honestPackaged` is `packagedLabel` of that pairing. `restrictionAgreesOnCenter_honest` takes related presentations, a common assignment, both `RespectsAt` hypotheses, and `Cv.K = Cw.K` at general `k`; the proof is `restrictionAgreesOnCenter_iff_source.mpr` then `LinearMap.ext` of the two restrictions to the same pairing, not `K=⊥` and not a transport unfolding. `starAccepts_honest` is the freeze identity form (`vs=ws`, `Rel.refl`, same honest labels, `fun i => rfl` on centers) proved by the same pairing `ext`, not a packaging-only alias of `starAccepts`. Rel is used to inhabit `restrictionAgreesOnCenter` / `LeafVertex.Rel.refl`; it is not an ingredient of the pairing identity. Checks `#check` every public name, `#print axioms` every public theorem, and invoke `honestRawLabel_eval_equation`, `honestRawLabel_respects_iff`, `restrictionAgreesOnCenter_honest` (distinct original-row Rel pair and empty self), and `starAccepts_honest` at n=1 and n=2 plus empty `J=h=0` zero assignment.

Notes: theorems quantify over general `k` with hypothesized shared `K`; Checks use `k=0` and `K=⊥` as the freeze allows, so `Hom(⊥, ZMod 2)` is unique and Check-level center agreement is automatic. Nonempty fixtures have `L=⊥`, so `PresentedLeaf.Rel` is `sup_comm` of equation spans. `starAccepts_honest` compares each leaf to itself; self-agreement holds for any equal labels, so the YES-specific force is `respects_iff` plus related-pair pairing, not identity star acceptance. Pairwise `hK : ∀ i j, (Cs i).K = (Cs j).K` is unused in the `starAccepts_honest` body (identity uses `rfl`). Module silences `linter.unusedVariables`. Empty `U=∅` `RespectsAt` is `False.elim` from `e ∈ ∅`. Checks prove queried original-row `badRow = false`, not a global gadget-inclusive `violations = 0`. Not credited: NP-hardness, Theorem 1, Corollary 2, NO/soundness decoder, SeededMap, P-versus-NP, publication.
