# Actual packed paddedRunGuardTag FP tag: non-claims-boundary review

**GO-WITH-NOTES.**

Frozen source `b8ba7b55b542759d151410667ed56e50f6dcad3e` (`prove actual paddedRunGuard packed FP tag`). Gate/closeout name `b8ba7b5`. Main `ActualSelectedCmmsaSeededMap.lean` SHA-256 `1B3BCB0452CD11DCB9C7B61E97F35FF8807297C0526F92534BF9DD2F3D7E69C9` (git blob `de09577c74a2910aa2b81a360a5b4c3b5272878f`, 14990 bytes, LF, git-object SHA-256 `857647D14BB529A289D8DB31B22BFFBE3607558D7A4B613F5BD8575A3D8454F8`). Checks SHA-256 `94D888D4645D8BD0EB4231F2766F8878030CDC3C585AC06B4F3A0B055E3344A8` (git blob `3f0fdc959eb9b333b79c142f4adc3a5cd3abd8c5`, 1759 bytes, LF, git-object SHA-256 `0750477F98BF0BE1F20A89F03D62B7E2C8EF5FAD69C69AF26D978334CADAC42B`). Evidence `lean/evidence/2026-09-16-actual-padded-run-guard-fp-fresh-run/` and closeout `lean/evidence/2026-09-16-actual-padded-run-guard-fp-closeout.md`. Independently `git cat-file`’d and rehashed those blobs at `b8ba7b5`; certify commit `cba404e`, `HEAD` (Lean byte-identical), and the working-tree Lean match the freeze and the closeout table. Certification commit `cba404e` is evidence-only and did not change the Lean sources. Recorded oleans `85487B040ABB6C47267693905D152392E9F461705078D09FB665F903FCC58F5B` / `FC2513F4FBD85CC8AEDBD10BA4CB5E53B51034A6C387A912D7DEDEDD0573D5D3` (local target-fresh twice after GCP idle-stop, matching hashes, exit 0). Closeout SHA-256 `3A6FE5996342843EAEACF61653ABE001EE24532F4C3BC49A7A1D71D500B06C9E` (git blob `70c26a452c1af4a8a38ccc608b43e18b3c4531a1`, 1590 bytes, LF). Local cert log git-blob SHA-256 `DF62B781EE8A5A0993A374B11E04FE36D6D27660194CFF49C401A4C61382103F` (blob `299a69bb12358fdd0f9caca9a4d5f21e1fa52ebf`, 2282 bytes, LF). GCP unavailability note SHA-256 `33DC5B4FE5CCC953C1C92F3337AC4C7871702F635174E95CD9FAEF6810D4C199` (git blob `00e31c52cd9fc00680de9718fffd32b4d31d4eec`). Parent Lean blob `9fa1a0ed2649fcb3424c62a861abd545f24b0357` (`88cee56` / `1b47db7`, SHA-256 `A0EFD901492C1D0DDB0012B3CDF545B06C51574812F5AC894257309E0B560786`). `MANUSCRIPT.md` unpublished bytes are unchanged (`9ec48580b55b3e34afad115199817da3c1b06eb3`, SHA-256 `FF00997C8C243E88982C41B0B0E24FAAAAFE36CEC6F1903824599DC242538686`). README, `CITATION.cff`, `lean/README.md`, `lean/PROVENANCE.md`, `lakefile.toml`, and `lean/PvNP.lean` are unchanged by this increment. This lens did not compile or edit Lean. Review is of git blobs at `b8ba7b5` only.

Accepted, freeze stop-loss only (packed `paddedRunGuardTag ∈ Complexity.FP` with decode + `coinRuler` length guard; no `selectedPairedRun_mem_FP`):

- `paddedRunGuardTag eps z` is a total `List Bool → List Bool` packing of the decode + `coinRuler` length guard of `paddedRunOption`. Failure (`decodeInput (pairFst z) = none`, or coin-tape length ≠ `coinRuler eps (pairFst z).length`) is `[]`. Success is `true :: pair (pairFst z) (pairSnd z)`. It does not execute `runOption` / `checkedBits` / `accepted`, does not check policy `precision` / `trials`, and does not emit a run output tree. `paddedRunGuardTag_mem_FP` places that packed function in `Complexity.FP` from Cobham composition of parent `decodeInputTag_mem_FP`, `selectedCoinRuler_mem_FP`, `emptyFlag`, `lenEqFlag`, `selectHead`, and `pair`. It is not un-packed `paddedRun ∈ FP` and not `selectedPairedRun_mem_FP`.
- `paddedRunGuardTag_eq` is the freeze semantic agreement: the packed tag equals `[]` on decode failure and equals the length-guarded `true :: pair instanceBits coins` payload on decode success. It is a packing-contract lemma, not `decodeInput ∈ FP` and not `paddedRunOption ∈ FP`.
- `paddedRunGuardTag_empty` is the empty-tape packing identity `paddedRunGuardTag eps [] = []`. It is not a named public theorem that `paddedRun ∈ FP`.
- Parent `selectedPairedRun L eps` remains the `paddedRun` wrapper on `pairFst` / `pairSnd`. There is still no `selectedPairedRun_mem_FP` and no `paddedRun_mem_FP`. Parent `selectedCoinRuler_mem_FP` remains the quadratic length-only ruler. Empty-tape `selectedPairedRun L (1/4) [] = []` remains in Checks.
- `selectedSeededMap` is not defined. `hSrcCmmsa` is not inhabited. There is no dummy 3SAT→CMMSA identity.

The module header denies `selectedPairedRun_mem_FP` (`runOption` / `checkedBits` / `accepted` / output `tree` are not on the Cobham FP surface), `selectedSeededMap`, inhabited `hSrcCmmsa`, dummy 3SAT→CMMSA identity, unconditional Theorem 1, Corollary 2, and P vs NP. Checks `#check` / `#print axioms` `selectedPairedRun`, `selectedCoinRuler`, `selectedCoinRuler_mem_FP`, `paddedRunGuardTag`, `paddedRunGuardTag_mem_FP`, `paddedRunGuardTag_eq`, and `paddedRunGuardTag_empty`; inhabit `selectedCoinRuler eps ∈ Complexity.FP` and `paddedRunGuardTag eps ∈ Complexity.FP` via `example : paddedRunGuardTag eps ∈ Complexity.FP := paddedRunGuardTag_mem_FP eps`; reduce ruler length to `coinRuler`; reduce empty-tape `selectedPairedRun L (1/4) [] = []`; and reduce `paddedRunGuardTag eps [] = []` via `paddedRunGuardTag_empty`. Checks do **not** `#check` `selectedPairedRun_mem_FP`, `selectedSeededMap`, or `hSrcCmmsa`, and do **not** inhabit un-packed `paddedRun ∈ Complexity.FP` or `selectedPairedRun ∈ Complexity.FP`. Main does not import `*Checks.lean`. `#print axioms paddedRunGuardTag_mem_FP` / `paddedRunGuardTag_eq` / `paddedRunGuardTag_empty` are `propext`, `Classical.choice`, `Quot.sound` only. Forbidden-scan of the freeze blobs is clean (`sorry` / `admit` / `native_decide` / new `axiom` absent; Theorem 1 / Corollary 2 / P vs NP / `selectedSeededMap` / `hSrcCmmsa` / dummy 3SAT identity appear only in denials). README, `CITATION.cff`, `lean/README.md`, `lean/PROVENANCE.md`, and `MANUSCRIPT.md` are unchanged by this increment.

Notes, not blocking:

- The Main module docstring frames the packed decode/`coinRuler` guard of `paddedRunOption` and still says `selectedPairedRun_mem_FP` is omitted. That is the file-goal catalog plus this increment’s packed membership, not a theorem that `paddedRun ∈ FP` is Theorem 1 material.
- The `paddedRunGuardTag` docstring says success payload is `true :: pair instanceBits coins` and failure is `[]`. That is the packing-contract line. The payload is the input pair, not a `runOption` output.
- Module/commit titles that say “actual paddedRunGuard packed FP tag” / Checks “packed decode/`coinRuler` guard of `paddedRunOption`” are catalog labels. They name packed `paddedRunGuardTag ∈ Complexity.FP`, not `selectedPairedRun_mem_FP`, not `selectedSeededMap`, not inhabited `hSrcCmmsa`, not unconditional Theorem 1, Corollary 2, publication, or `P` versus `NP`.
- Closeout phrasing “packed decode + `coinRuler` length guard of `paddedRunOption`” is `paddedRunGuardTag_mem_FP` / `paddedRunGuardTag_eq`. Closeout “Not `selectedPairedRun_mem_FP`. Not Theorem 1.” keeps the forbidden classes closed.
- Private `instTag` / `instTag_mem_FP` / `lenEqFlagFn_mem_FP` support the public packing and membership. They are not additional public claims and are not `selectedPairedRun_mem_FP`.
- Private `splitNode` / `nodeLeft` / `nodeRight` / `instEnc` / `weightsEnc` / `rowsEnc` / `paramsEnc` / `precisionEnc` / `trialsEnc` / `weightsLenBits` / `rowsLenBits` and their `_mem_FP` / `_some` lemmas are compiled private scaffolding toward later policy-field packing (`precision` / `trials` / row and weight lengths). They are unused by the public tag, are not Checks-driven, and are not `selectedPairedRun_mem_FP`.
- Semantic `paddedRunOption` still also requires policy `precision` / `trials` equalities, `trials * precision ≤ coins.length`, and `runOption`. This increment packs only decode-success plus the `coinRuler` length conjunct. That is the freeze-permitted stop-loss, not a packing of the executor.
- The empty-tape Checks examples remain runtime packing identities `selectedPairedRun L (1/4) [] = []` and `paddedRunGuardTag eps [] = []`. They are not a named public theorem that `selectedPairedRun ∈ FP`.
- `lean/README.md` and `lean/PROVENANCE.md` still list `decodeInputTag_mem_FP` among what is not claimed / remaining work. That catalog is stale relative to the prior packed theorem and is conservative, not a claims expansion. This increment does not edit those files and does not convert the packed guard into manuscript Theorem 1.
- Certification was local target-fresh of frozen `b8ba7b5` blobs after GCP `quantyra-lean-builder-01` started then idle-stopped (manuscript-repo content-addressed cloud gate not ported). The closeout records that limit. This lens does not treat local-vs-GCP as a claims expansion.
- Stopping after packed `paddedRunGuardTag_mem_FP` / `paddedRunGuardTag_eq` is the freeze-permitted stop-loss. It does not expand the claim boundary to `selectedPairedRun_mem_FP`, `selectedSeededMap`, or the rest of the SeededMap route.

Forbidden claims absent (`selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, dummy 3SAT→CMMSA identity, unconditional Theorem 1, Corollary 2, P vs NP, publication/DOI). Next: pack `runOption` / `checkedBits` then `selectedPairedRun_mem_FP` then `selectedSeededMap`. Then a 3SAT (or 3-Lin) → `encodeInput` table compiler ∈ FP with `MapReducesVia` into a selected pipeline promise. Do not inhabit `hSrcCmmsa` with identity, and do not skip to Theorem 1.
