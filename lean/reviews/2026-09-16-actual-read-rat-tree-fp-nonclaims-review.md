# Actual readRatTag tree agreement: non-claims-boundary review

**GO-WITH-NOTES.**

Frozen source `93d35131ed5317209ab456651387d4d9a2558fc2` (`prove actual readRatTag tree agreement`). Gate/closeout name `93d3513`. Main `ActualDecodeInputFP.lean` SHA-256 `6356E80AF6A35BF9725434C911961B130A2BAD86F46B42951A7E2D85FB2907D4` (git blob `c32c15224f13c7196fa4491991ca6f10cffbe2bc`, 149949 bytes, LF, git-object SHA-256 `A13687A20DECB27E902380A15E6F67697F33366CCA6B54B9DC00F5168F5FED24`). Checks SHA-256 `0EA0879E4232070A818E453FB0885A1934BFC29709391DA022817038088593E1` (git blob `0943f4d1fe396732764c44ebbab0c8d54b807d20`, 1583 bytes, LF, git-object SHA-256 `28E39315D3A17F0320F8A31371869810CB00431C7A121B4A99A936D8179593C9`). Evidence `research/evidence/2026-09-16-actual-read-rat-tree-fp-fresh-run/` and closeout `research/evidence/2026-09-16-actual-read-rat-tree-fp-closeout.md`. Independently rehashed git blobs at `93d3513`, certify commit `4ca1d89`, and `HEAD` (Lean byte-identical) match the freeze and the closeout table. Certification commit `4ca1d89` did not change the Lean sources. The working-tree Main/Checks are dirty (unpublished `readSignedTag` / `_mem_FP` / `_of_tree` plus CRLF) and are **not** the reviewed source. Recorded oleans `825FD451B5E459A111AAEC37238062F373E9FEE1869DA4A373341E91F6DA1BFB` / `F6DFD04AD74F078DA70BE8F9FDECE26F07F9B4711882585EB1522F09A6AEC209` (local target-fresh twice, matching hashes, exit 0). Closeout SHA-256 `D92B2C67212583E45C6BF2256C631B10B3155FF3D5A3F8C01F32A9C3B92626C2`. Local cert log SHA-256 `1133F3AC6CD264CF6A6FC147B0AB4CA980CE0B62DE4F5E8C96C185701E9C9DB8`. GCP unavailability note SHA-256 `13200AA6AD4511676DF7B336AD2036CD4C9DAA880BF65798309F9E6D106D3702`. Parent Lean blob `ef6e01beed60801771de756c718657929782621c` (`91d8de7`). `ActualSelectedCmmsaSeededMap.lean` is untouched (`38d9ad0`, blob `9fa1a0ed2649fcb3424c62a861abd545f24b0357`).

Accepted, freeze stop-loss only (`readRatTag_of_tree`; packed `readRatTag` on `encode t` agrees with semantic `readRat` / `ratTree`; no `decodeInput ∈ FP`):

- `readRatTag_of_tree` is the freeze statement: `readRatTag (CMMSACodec.Tree.encode t)` equals `[]` when `readRat t = none`, and `true :: encode (ratTree q)` when `readRat t = some q`. It is agreement on a complete encoded tree, not `readRat ∈ FP`, not `readRatTag = pack ∘ readRat` on malformed/truncated tapes, and not `decodeInputTag ∈ FP`.
- `gcdBits_odd_pair` remains from the parent `91d8de7` surface: packed GCD of a *canonical* pair with nonempty first component and at least one odd argument (or a zero second component) agrees with `Nat.gcd`. It is not `gcdBits = encode ∘ gcd` on arbitrary tapes and not `GCD ∈ FP` as a number-theoretic map.
- `readRatTag_mem_FP` / `gcdBits_mem_FP` remain packed Cobham memberships from prior certified increments. They are not un-packed semantic readers.
- Public packing of `decodeInput` exists as freeze-permitted progress: `decodeInputTag` / `_empty` / `_none` / `_some`. There is no public `decodeInputTag_mem_FP`, no `decodeInput ∈ FP`, and no Checks example inhabiting `decodeInputTag ∈ Complexity.FP`. `decodeInputTag_mem_FP_of_read` is a private conditional (`readInputTag ∈ FP → decodeInputTag ∈ FP`), not an unconditional membership.
- Private `gcdShared_eq` / `packReduced_eq` / `readRatOnEncode_of_tree` / `readDigitsTag_of_tree` support the public tree agreement. They are not additional public claims and are not `decodeInputTag_mem_FP`.
- `selectedSeededMap` is not defined. `hSrcCmmsa` is not inhabited. There is no dummy 3SAT→CMMSA identity. `ActualSelectedCmmsaSeededMap.lean` is untouched.

The module header denies `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, and P vs NP, and states that the module does not claim `decodeInputTag ∈ FP` until `decodeInputTag_mem_FP`. Checks `#check` / `#print axioms` `decodeInputTag`, `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits`, `gcdBits_mem_FP`, `gcdBits_odd_pair`, `readRatTag`, `readRatTag_mem_FP`, and `readRatTag_of_tree`; inhabit `gcdBits ∈ Complexity.FP` and `readRatTag ∈ Complexity.FP`; reduce `readRatTag (encode leaf) = []` from `readRatTag_of_tree`; and reduce `decodeInputTag [] = []` and truncated `decodeInputTag [true] = []`. Checks do **not** `#check` `decodeInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, or `hSrcCmmsa`, and do **not** inhabit `decodeInputTag ∈ Complexity.FP`. Main does not import `*Checks.lean`. `#print axioms readRatTag_of_tree` is `propext`, `Classical.choice`, `Quot.sound` only. Forbidden-scan of the freeze blobs is clean (`sorry` / `admit` / `native_decide` / new `axiom` absent; Theorem 1 / Corollary 2 / P vs NP appear only in denials). README, `INTEGRITY-CLAIMS.md`, `CHANGELOG.md`, and `CITATION.cff` are unchanged by this increment.

Notes, not blocking:

- The Main module docstring still frames packed helpers *toward* `ExecutablePipelineInput.decodeInput` and says `gcdBits` / `readRatTag` are in `FP`. That is the file-goal catalog line plus the already-certified packed memberships, not a theorem that `decodeInput ∈ FP` or `decodeInputTag ∈ FP`. The Checks header and the closeout keep `decodeInputTag_mem_FP` open.
- Module/commit titles that say "actual readRatTag tree agreement" / `ActualDecodeInputFP` / Checks "packed decodeInput FP tag" are catalog labels. They name `readRatTag_of_tree` (and the already-certified packed `readRatTag ∈ FP`), not un-packed `readRat ∈ FP`, not `decodeInput ∈ FP`, not `selectedSeededMap`, not inhabited `hSrcCmmsa`, not unconditional Theorem 1, Corollary 2, publication, or `P` versus `NP`.
- Closeout phrasing "packed `readRatTag` on `encode t` agrees with semantic `readRat` / `ratTree`" is the public theorem. Do not cite it as `readRat ∈ FP` or as `decodeInput ∈ FP`. Closeout "Also `gcdBits_odd_pair`" restates the parent public odd-pair restriction still present in this freeze; it is not a new general GCD identity.
- The Checks leaf example inhabits the `none` arm of `readRatTag_of_tree` (`readRat leaf = none`). The `some q` / `ratTree` arm is in the theorem, not in a Checks numeric inhabitant.
- Private `gcdShared_eq` is `Nat.gcd` of the remaining parts after `shareTwos`/`gcdPrep` on canonical tapes. Private `packReduced_eq` rebuilds `true :: encode (ratTree (bitValue a / bitValue b))` when `bitValue b ≠ 0`. Do not cite either as a public theorem or as `decodeInputTag_mem_FP`.
- The truncated-`true` check is a runtime packing identity `decodeInputTag [true] = []`. It is not a named public FP theorem and is not `decodeInput ∈ FP`.
- Certification was local target-fresh of frozen `93d3513` blobs after GCP `quantyra-lean-builder-01` reauthentication failure. The closeout records that limit. This lens does not treat local-vs-GCP as a claims expansion.
- A dirty working tree already adds `readSignedTag`. That work is outside `93d3513` and is not accepted by this review.

Forbidden claims absent (`decodeInput ∈ FP`, Theorem 1, Corollary 2, P vs NP). The freeze’s `readSigned` / `readList` / `readFormula` / `readTable` / `readInputTag_mem_FP` / `decodeInputTag_mem_FP` target remains open. Next: `decodeInputTag_mem_FP` via `readInputTag ∈ FP`. Then `selectedPairedRun_mem_FP` and `selectedSeededMap`. Then a 3SAT (or 3-Lin) → `encodeInput` table compiler ∈ FP with `MapReducesVia` into a selected pipeline promise. Do not inhabit `hSrcCmmsa` with identity, do not sorry `decodeInputTag_mem_FP`, and do not skip to Theorem 1.
