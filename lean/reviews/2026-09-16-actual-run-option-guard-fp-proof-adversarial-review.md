# Actual runOptionGuard packed FP tag proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `c78bf1a5b4c8c31fe82535f3f293c57c2b348da2` (`prove actual runOptionGuard packed FP tag`). Parent `374e1bad81688a6b27e115a5b9b1efa4a84f7dfd` (`review actual paddedRunGuard packed FP tag`; Lean parent blob still `de09577c74a2910aa2b81a360a5b4c3b5272878f` from `b8ba7b5`). Main SHA-256 `99B5245219A98031C9BA4537E19E2968FF29AD0327F41153CBF8B3275D5EB152`, Checks `FAF432B65CAC722C809646E2979EB8F2CEB43037D7018944C35DD64A055F38D8`. Independent `git cat-file` SHA-256 of both Lean blobs at `c78bf1a` match the freeze and closeout (`blob 7fe2f4c6bf7faffee4ea20d70b0cb108f57941a6` / `373bbdbd75f65f21c7b48867d973c600ea0dc224`, 35975 / 2183 bytes, LF-only, 859 / 61 newlines, zero CR). Recomputed git-ids of those extracted blobs reproduce the same ids. Certification `01e599122f2a345e3c1cd4ab3bde3f35ef4d8d58` (`certify actual runOptionGuard packed FP tag (local after GCP idle-stop)`) adds evidence only; Lean blobs unchanged from `c78bf1a` (`git diff --name-only c78bf1a 01e5991 -- '*.lean'` empty; same blobs at `HEAD`). GCP `quantyra-lean-builder-01` TERMINATED (idle-stopped after the previous paddedRunGuard start; manuscript-repo content-addressed cloud gate not ported); local target-fresh of frozen blobs twice, main+Checks exit 0, objects `960B9AB41DA7C6BF5621A2BB285FA926A1D43F34AEE63E2E5AF7352F7BFB1651` / `6C841EBD06E28B9E470E600F26517364C4A270E49083B5A1F8B630BCC8CD4B9B`, `MATCH_MAIN=True MATCH_CHECKS=True`. Printed axioms of `runOptionGuardTag_mem_FP`, `runOptionGuardTag_none`, and `runOptionGuardTag_empty` only `propext`, `Classical.choice`, `Quot.sound`. `ActualDecodeInputFP.lean` untouched (`blob aba9e488124928fba7d18fbb22f6d056b20c8eff`, SHA-256 `D2405DC2CE759190671801AEF669297C1F8DD9F56BAC1079F7E95D9EB87DDD87`, same as `f7f338b` / `b8ba7b5`). `ExecutablePipelineInput.lean` / `ExecutableSamplingPolicy.lean` blobs `f4593745` / `0a2583db` unchanged. Working-tree Main/Checks are byte-identical LF with the freeze hashes above. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze stop-loss: pack the decode + unary `trials*precision` length guard of `runOption`, prove `runOptionGuardTag ∈ FP`, then stop. Public surface that compiled: `selectedPairedRun` (def), `selectedCoinRuler` / `_mem_FP` (parent), `paddedRunGuardTag` / `_mem_FP` / `_eq` / `_empty` (parent), `runOptionGuardTag` (def), `runOptionGuardTag_mem_FP`, `runOptionGuardTag_none`, `runOptionGuardTag_empty`. There is no `runOptionGuardTag_eq`. `runOptionTag` / `runOptionTag_mem_FP` are not declared. `selectedPairedRun_mem_FP` is omitted; `selectedSeededMap` is not defined. Packed walk: `instTag = decodeInputTag ∘ pairFst`; fail-closed `selectHead (emptyFlag (instTag z)) []` on decode failure; inner `selectHead` of `lenEqFlag (pairSnd z) (trialsPrecisionRuler z)` keeps `true :: pair (pairFst z) (pairSnd z)` on length match and `[]` otherwise. `trialsPrecisionRuler z` is `replicate (|precisionUnary z| * |trialsUnary z|) false`. Each unary is `natUnaryEnc` of a bounded `nuStep` iterate on `precisionEnc` / `trialsEnc` (the `natTree` fields of a successful `decodeInputTag`): little-endian digit tags `leaf` / `node leaf leaf` match `digitTree`, `pow` starts at unary `1`, a true digit adds current `pow` to `acc` then doubles, a false digit only doubles, a remaining leaf is `nuDone` (`flag = [false]`) and any malformed spine is `nuFail` (`flag = [true]`). `natUnaryEnc` emits `nuAcc` only on `flag = [false]`, else `[]`. Acc/`pow` are `takeLen`-clamped to `coinsCap = pairSnd z ++ [false]` (length `|coins|+1`). `runOptionGuardTag_mem_FP` is `selectHeadFn_mem_FP` / `emptyFlagFn_mem_FP` of already-certified `decodeInputTag_mem_FP`, pairing-block `fst`/`snd`, `pairFn_mem_FP`, `cons_mem_FP true`, and `trialsPrecisionRuler_mem_FP`. The ruler is `mulLenFn_mem_FP` of two `natUnaryEnc_mem_FP`s, each `iterate_mem_FP` of `nuStep_mem_FP` with polynomial `nuIterRuler` / `nuWidth` and a `NuReach` length invariant on every prefix (`st_le` via `nuPack_length` against width `16(|z|+|decodeInputTag|+|coinsCap|+1)`). `runOptionGuardTag_none` is only the decode-`none` arm (`instTag z = []` then `emptyFlag_nil`). `runOptionGuardTag_empty` is that arm on `[]` via `decodeInput_empty`. Checks `#check` / `#print axioms` the parent ruler/guard plus the three new names, inhabit `runOptionGuardTag ∈ FP`, and reduce `runOptionGuardTag [] = []`.

Hunts (absent). `sorry` on packed decode+unary `trials*precision` guard FP: no `sorry` / `admit` / `native_decide` / new `axiom` in main or Checks at `c78bf1a`; no `decide` in the new region. Claiming `runOptionTag_mem_FP` or un-packed `runOption ∈ FP`: the public theorem is `runOptionGuardTag ∈ Complexity.FP`, the packed `List Bool → List Bool` tag, not `runOption` / `checkedBits` / `accepted` / output `tree`; Checks inhabit `runOptionGuardTag ∈ Complexity.FP` and do not inhabit `runOption ∈ FP` or `selectedPairedRun ∈ FP`. Dummy identity: `constFn_mem_FP []` is the fail / still-running / `nuFail` arm of `runOptionGuardTag` / `natUnaryEnc`; `id_mem_FP` is the stay-put branch of `nuStep` when `nuFlag` is already nonempty, not `fpSeededMap` of `id`, `identitySeededMap`, or a 3SAT→CMMSA compiler. Vacuous membership: the success payload is `true :: pair instanceBits coins` on a nonempty length-match, not a restriction of the theorem to `[]`; `runOptionGuardTag_none` is honestly named and does not pretend to be `_eq`. The unary walker is not a constant-empty dummy: `nuIterRuler` always includes `[false]` so at least one step runs, `natTree 0` is a leaf that `nuDone`s with empty `acc` (unary 0), and the digit tags are the `digitTree` tags. Checks not driving the real theorems: Checks `#check` / `#print axioms` / inhabit `runOptionGuardTag_mem_FP`. New axioms: none; printed set is the standard trio. Theorem 1 / `hSrcCmmsa`: not inhabited.

Notes: There is no public `runOptionGuardTag_eq` relating `|trialsPrecisionRuler z|` to semantic `x.trials * x.precision`. Do not credit that lemma. `NuReach` bounds state size for `iterate_mem_FP`; it does not prove the iterate reaches `nuDone`, that `precisionUnary` / `trialsUnary` have those Nat lengths, or that `|coins|+1` clamping is a no-op on well-formed instances. Checks coverage of the new tag is the empty `[]` example only; there is no `rfl` on a concrete well-formed `decodeInput` success with matching `trials*precision` coin length. Nonempty `runOptionGuardTag` does not imply `runOption ≠ none`: the tag never runs `checkedBits` / `accepted` / output `tree`. Private `weightsEnc` / `rowsEnc` / `paramsEnc` / `weightsLenBits` / `rowsLenBits` remain unused by the public theorems. `open RandomizedReduction` / `ActualHeadlineParameters` / `ActualSatToThreeSatSource` remain unused leftovers from the intended full SeededMap increment. Main does not import `*Checks.lean`. Stopping after `runOptionGuardTag_mem_FP` / `_none` / `_empty` is the freeze-permitted stop-loss. Next consumer remains `runOptionGuardTag_eq` then packed `checkedBits` / `accepted` / `tree` / `bits`, then `runOptionTag_mem_FP` and `selectedPairedRun_mem_FP`; do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `runOptionGuardTag_eq`, `runOptionTag_mem_FP`, `checkedBits` / `accepted` / `tree` packing, `selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, dummy 3SAT→CMMSA identity, unconditional Theorem 1, Corollary 2, P versus NP, un-packed `runOption ∈ FP`, publication.
