# Actual packed readRatTag FP tag proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `dbdca8728e164f41d9fbe005ab699f48450c67f4` (`prove actual readRat packed FP tag`). Main `2508B3F45C37D04889D3B6448F0DAF44E8842F625B8D778D743E3A920D487763`, Checks `9380B508A6BF71A74311AA8B6884921982D9B8CB3CB0DDC4D43FE80DC65E0B7A`. Certification `0a9b66e2e797c9027304829548e3cdd82f19e823`. Gate named `dbdca87` (`gate.sh` pins `EXPECTED_HEAD=dbdca8728…`, `EXPECTED_MAIN=2508B3F4…`, `EXPECTED_CHECKS=9380B508…`; bundle `F06511A2…`). 26 stages exit 0 (`exception-repair` through `actual-tree-parse-fp`, main×2, checks×2); objects `0F4729C564C61B7DF5B47091C59D826D3B13A7874553BEB0CDFD032D6EAC83DB` / `D22359AA6A705B5FB352C0A8573A5B1B113EEF59AC896DE0AC7369C3AB7A36C9`. Independent `git cat-file` SHA-256 of both Lean blobs at `dbdca87` match the freeze and the gate (`blob f1c8801e1e58edfa852904e018f0b762462926dd` / `77e607bf3b607fb7b278d731da08ca178acfbc47`, 95149 / 1248 bytes, LF-only). Cert commit `0a9b66e` adds evidence only; Lean blobs unchanged from `dbdca87` (`git diff --name-only dbdca87 0a9b66e -- '*.lean'` empty). Working-tree Checks SHA-256 matches; working-tree Main differs by later uncommitted Luna edits (`97615` bytes, SHA `0E6D6B17085D3C0A2185BAA56942EEAF5957CC4F17D30EEB1628CFC8EE5FB79C`, including a post-freeze `quotBits_eq` / `DivInv` block). Those edits are not part of this review. Independent rehash `result=PASS`, 175 rows 0 mismatches. Forbidden scan clean. Printed axioms of `readRatTag_mem_FP` only `propext`, `Classical.choice`, `Quot.sound`; `gcdBits_mem_FP` and `decodeInputTag_empty` / `_none` / `_some` print the same trio. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze stop-loss: packed `readRatTag` in `FP`, then stop, and do not sorry `decodeInputTag_mem_FP`. Public surface that compiled: `decodeInputTag` (def), `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits` (def), `gcdBits_mem_FP`, `readRatTag` (def), `readRatTag_mem_FP`. `readRatTag_of_tree` is not declared; `decodeInputTag_mem_FP` is not declared; `selectedSeededMap` is not defined. `readRatTag z` is a total `List Bool → List Bool`: empty `treeParseTag` → `[]`; leftover after a parse (`pairSnd` of `dropOne treeParseTag` nonempty) → `[]`; else `readRatOnEncode` of the parsed tree tape. `readRatOnEncode` is empty on empty left/right digit tags or on `stripTrailing` den = `[]` (den=0); else `packReduced` of the stripped digit tapes. `packReduced a b` is `[true, true] ++ encodeDigits (quotBits (fst (shareTwos a' b')) (gcdShared a' b')) ++ encodeDigits (quotBits (snd (shareTwos a' b')) (gcdShared a' b'))` with `a' = stripTrailing a`. That bit-pattern is the intended option packing `true :: encode (node (digitTree num) (digitTree den))`, matching `true :: encode (ratTree q)` *shape*, not a proved equality. `readRatTag_mem_FP` is Cobham `selectHeadFn` / `mem_FP_comp` of `treeParseTag_mem_FP`, `readRatOnEncode_mem_FP`, and `constFn []`. `readRatOnEncode_mem_FP` composes `nodeLeft`/`nodeRight`, `readDigitsTag`, `stripTrailing`, and `packReduced_mem_FP`. `packReduced_mem_FP` composes `shareTwos` (`iterate_mem_FP` of `share2Step` with ruler `|a++b|`), `gcdShared` (`gcdBits` after `gcdPrep`), `quotBits`, and `encodeDigits`. `gcdPrep` swaps an empty `pairFst` so `gcdBits` of `pair [] b` is not stuck at `[]`. Checks `#check` / `#print axioms` the decodeInputTag names plus `gcdBits` / `gcdBits_mem_FP` / `readRatTag` / `readRatTag_mem_FP`, inhabit `gcdBits ∈ FP` and `readRatTag ∈ FP`, and reduce `decodeInputTag []` / truncated `decodeInputTag [true] = []`.

Hunts (absent). `sorry` on readRat FP: no `sorry` / `admit` / `native_decide` / new `axiom` in main or Checks at `dbdca87`; the only `decide` is `Nat.div_lt_self` fuel in private `oddPartFuel_eq_of_enough`. Claiming semantic `ratTree` agreement: there is no `readRatTag_of_tree`; `readDigitsTag_of_tree` / `readNatTag_of_tree` remain the last packed-tree equalities. Private `gcdStep_oddPart` / `gcdStep_iterate_oddPart` / `dropTwos_oddPart` preserve `Nat.gcd` of `oddPart` along `gcdStep` and identify `dropTwos` with `oddPart`, but they are not used to prove `gcdBits = encode ∘ gcd`, `quotBits a b = (bitValue a / bitValue b).bits`, or `readRatTag (encode t) = match readRat t with | none => [] | some q => true :: encode (ratTree q)`. The `readRatTag` docstring states that packing as intent; it is not a theorem. Dummy identity: `id_mem_FP` is the Cobham init / stay-put branch of `gcdStep`, `share2Step`, and `gcdPrep`; `constFn_mem_FP []` is the malformed/den=0 branch; neither is `fpSeededMap` of `id`, `identitySeededMap`, nor a 3SAT→CMMSA compiler. `ActualSelectedCmmsaSeededMap.lean` is untouched (`blob 9fa1a0ed`, commit `38d9ad0`). New axioms: none; printed set is the standard trio.

Notes: Checks coverage of `readRatTag` is membership only; there is no `rfl` example on a concrete rational tree. `gcdBits` remains the truncated Stein-like iterate from the prior stop-loss: ruler `2|z|+16` is polynomial and used, not a stuck-GCD theorem; `gcdPrep` only repairs empty-fst stay-put. `shareTwos` drops matching low `false` bits and is not proved to extract `min(v2(a),v2(b))`. `quotBits` is a packed restoring-division iterate; at this freeze it has membership, not `Nat.div` agreement (a later uncommitted `quotBits_eq` block is out of scope). Module header still describes packed `decodeInput` in `FP`; Checks header is the honest boundary (`gcdBits_mem_FP` and `readRatTag_mem_FP` on the Cobham surface; `decodeInputTag_mem_FP` remains). Unused `simp` arguments and an unused `omega` warn on main compile and do not change the theorems. `packReduced_mem_FP` raises `maxHeartbeats` to `1000000`; that is a kernel budget, not a cheat. `decodeInputTag_mem_FP_of_read` is still a private implication that needs `readInputTag ∈ FP`. Main does not import `*Checks.lean`. Stopping after `readRatTag_mem_FP` is the freeze-permitted stop-loss. Next consumer remains `readRatTag_of_tree` (semantic GCD of the iterate, `quotBits` of the reduced pair, then `true :: encode (ratTree q)`), then `decodeInputTag_mem_FP`; do not sorry those, and do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `readRatTag_of_tree`, `decodeInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, P versus NP, publication.
