# Star acceptance proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `5606cd972f47575fe592945bcd4d47c0dcc5d3f2`. Main `1186473C…C7A5`, Checks `ED0E8DA9…E585`. Certification `d3d44f8`. Gate named `5606cd9` not `bba6dbb`. 28 stages exit 0 including `RejectionUnionChecks`; main+Checks twice matching; objects `03A3F72E…66F0` / `88FF0F54…7994`. Independent source hashes match. Independent rehash `result=PASS`, 187 rows 0 mismatches. First cert attempt failed because Checks imported `RejectionUnionChecks` without a compile stage; rerun added that stage; sources unchanged. Forbidden scan clean. Printed axioms only `propext`, `Classical.choice`, `Quot.sound`.

Freeze public statements compiled. `starAccepts` is `∀ i, restrictionAgreesOnCenter` (transported versus queried restriction on hypothesized shared `K`). `starAccepts_iff_anyFail_zero` is a classical split on `anyFailIndicator`'s `if ∃ ¬agree then 1 else 0`: fail branch is `False.elim` versus `1 = 0`; no-fail branch is `rfl` versus `by_contra` of the existential; not a tautology and not a soundness law. `starAccepts_of_source_agrees` is pointwise `restrictionAgreesOnCenter_iff_source.mpr`, not source-to-star soundness and not a CSP. `starAcceptsCenter` is the constant-`vs` form; `starAcceptsCenter_iff_starAccepts` is `Iff.rfl` as freeze allowed. No `Star` structure, no projection fibre, no proved coercion to `StarListDecoding.Star.accepts`, no PMF. Checks `#check` every public name, `#print axioms` every public theorem, and invoke `starAccepts_iff_anyFail_zero` and `starAccepts_of_source_agrees` on the freeze fixtures (identity Rel, distinct Rel pair, n=1 and n=2, empty `J=h=0` at n=0 and n=1); invoke `starAcceptsCenter_iff_starAccepts` on n=1 identity, n=2 distinct Rel, and both empty cases.

Notes: nonempty fixtures have `L=⊥` and `K=⊥`, so `Hom(⊥, ZMod 2)` is unique and every fail indicator is 0; `midPackaged` is the transport of `sourcePackaged`, so the distinct-Rel pair agrees by construction and no failing leaf is exhibited (the fail branch of the iff is kernel-checked, not fixture-inhabited). n=0 `of_source_agrees` is `Fin.elim0`. Empty Check is a vertex against itself. `starAcceptsCenter` is inhabited only via the iff, not as a standalone example. Module silences `linter.unusedVariables`; binders here are used. Not credited: hardness, Theorem 1, Corollary 2, P-versus-NP, publication.
