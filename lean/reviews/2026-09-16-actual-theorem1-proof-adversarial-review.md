# Actual Theorem 1 from two-stage Preserves proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `10c3b9b9653852f56e300fc2b8cc202ece2dbf71` (`prove actual theorem1 from two-stage Preserves`). Main `C62AAE87A1A4319198D648A431206750C3F48F8562590DAB56DDBA962678ABAF`, Checks `CE241ECFC419AC0D93E7A703B7980BBE92BFDD4613F6EDB6452A1E5524FF2029`. Certification `024bf2fcf8b52a731a8400669f985751075bbd01`. Gate named `10c3b9b` not `bba6dbb` (`certify.sh` rejects the sibling hash; `gate.sh` pins `EXPECTED_HEAD=10c3b9b…`). 10 stages exit 0 (`exception-repair`, `formula`, `cmmsa-codec`, `randomized-reduction`, `randomized-reduction-assembly`, `actual-cmmsa-randomized-reduction`, main+Checks twice); objects `56CC9970C1B32D89955A06D5B8654CA9009B4E289FCB592C64D070FCF4537C3C` / `61B212CF9774CD6EF0EB449E307ED1F326C2D5D0D16A080CC1108E8B7CE520CD`. Independent working-tree SHA-256 of both Lean sources match the freeze and the gate. Cert commit adds evidence only; Lean blobs unchanged from `10c3b9b`. Independent rehash `result=PASS`, 79 rows 0 mismatches. Forbidden scan clean. Printed axioms only `propext`, `Classical.choice`, `Quot.sound`. I made no Lean-source or Git changes and ran no additional broad build.

Freeze public statements compiled. `fpSeededMap` is zero-coin `run = f ∘ pairFst`, `ruler = fun _ => []`, `coinCount = 0`; `apply` ignores the seed (`pairFst_pair`). `fpSeededMap_success` is `eventProb_univ` on `Fin 0 → Bool` once `f x ∈ S`. `fpSeededMap_preserves` turns `MapReducesVia` (yes→yes and no→no) into `Preserves _ _ _ 0 0` with `1-0 ≤ 1`. `np_to_sat_seeded` unpacks `SAT.NPHard_language` (`∀ A ∈ NP, A ≤ₚ SAT.language`, i.e. `∃ f ∈ FP, ∀ x, x ∈ A ↔ f x ∈ SAT.language`) and feeds the Karp iff into `ofLanguage` sides (`yes = A`, `no = Aᶜ`; no-side is `mt (hiff x).mpr`). Cook–Levin stays at error `0`. `theorem1_realizable_cmmsa` still takes `hSatSrc` and `hSrcCmmsa` as `∃` `Preserves (1/6)` hypotheses. Route: `np_to_sat_seeded` at `0`, `exists_preserving_composition` of that map with `hSatSrc` at native `0 0 (1/6) (1/6)` (the six `norm_num`s are `0≤0`, `0≤0`, `0≤1/6`, `0≤1/6`, `1/6≤1`, `1/6≤1`, not a `0≤1` lift of Cook–Levin), `convert` of `0+1/6=1/6`, then `two_stage_randomized_map` at `1/6+1/6=1/3`. Total error is `0+1/6+1/6=1/3`, matching `RandomizedMapReduces`. Cook–Levin is not lifted from `0` to `1/6` (that would compose to `1/2` and miss the `1/3` contract). `identitySeededMap` is not used as `hSrcCmmsa`; identity preserves `P` to `P` at `0`, not `source` to `cmmsaPromise`. Checks `#check` / `#print axioms` every public theorem, run `fpSeededMap id` on `[]` and on `[true,false]` with a nonempty ignored seed, and inhabit `np_to_sat_seeded` on `SAT.language` via `language_mem_NP` and `NPComplete_language.1` at error `0`. Checks do not inhabit `theorem1_realizable_cmmsa`.

Hunts (absent). Lifting Cook–Levin to `1/6`: not done; first hop is composed at `0` and the sum is rewritten by equality, not `preserves_mono`. Dummy identity as `hSrcCmmsa`: not present in main or Checks; setting `source = cmmsaPromise` would still leave `hSatSrc` as a SAT-to-CMMSA `Preserves (1/6)` obligation. Checks inhabiting `theorem1` unconditionally: not present; SAT-to-SAT examples are Cook–Levin at `0` on `ofLanguage SAT.language`, not a discharge of `hSatSrc`/`hSrcCmmsa`. Empty `source` does not vacuously inhabit `hSatSrc`: SAT yes/no sides are total (`ofLanguage`), and `Preserves` to an empty target side would require `5/6 ≤ 0`.

Notes: the module docstring names “Manuscript Theorem 1” while the type remains conditional on the two uninhabited `1/6` maps and on caller-supplied `L`/`sig`/`gam` with only `1 ≤ sig` and `0 < gam < 1`, not manuscript `σ_L`/`γ_L`. That is a MEDIUM naming overreach, not a false kernel statement. First-hop coin/clock witnesses are discarded (`_, _`); `RandomizedMapReduces` only stores `Preserves` at `1/3`, as in the certified `c058348` interface. Checks coverage of `theorem1_realizable_cmmsa` is `#check`/`#print axioms` only. `fpSeededMap_coinCount` is `rfl` but still prints the standard classical axioms through `SeededMap`/`FP`. Module does not import `*Checks.lean`. No `sorry`/`admit`/`native_decide`/new `axiom`.

No HIGH vacuity, hypothesis, or statement bug. Not credited: unconditional manuscript Theorem 1, Corollary 2, inhabited `RandomizedPromiseNPHard (cmmsaPromise …)` without the two `1/6` maps, SAT-to-source or source-to-CMMSA `Preserves (1/6)` constructions, manuscript `σ_L`/`γ_L`, P versus NP, publication.
