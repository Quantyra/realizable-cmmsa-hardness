# Actual readRatTag tree-agreement proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `93d35131ed5317209ab456651387d4d9a2558fc2` (`prove actual readRatTag tree agreement`). Parent `91d8de75aa028847c37b57f0c0fabb302959d2b9` (`prove actual gcdBits odd-pair semantics`). Main SHA-256 `6356E80AF6A35BF9725434C911961B130A2BAD86F46B42951A7E2D85FB2907D4`, Checks `0EA0879E4232070A818E453FB0885A1934BFC29709391DA022817038088593E1`. Independent `git cat-file` SHA-256 of both Lean blobs at `93d3513` match the freeze and closeout (`blob c32c15224f13c7196fa4491991ca6f10cffbe2bc` / `0943f4d1fe396732764c44ebbab0c8d54b807d20`, 149949 / 1583 bytes, LF-only). `git hash-object` of those extracted blobs reproduces the same ids. Certification `4ca1d893d3685ff7a6c8dfe803107076cd0f07d9` / log-add `453fdf4e028c586b709bfc8f693e09db9bb51b17` add evidence only; Lean blobs unchanged from `93d3513` (`git diff --name-only 93d3513 HEAD -- '*.lean'` empty). GCP `quantyra-lean-builder-01` unavailable (`Reauthentication failed`); local target-fresh of frozen blobs twice, main+Checks exit 0, objects `825FD451B5E459A111AAEC37238062F373E9FEE1869DA4A373341E91F6DA1BFB` / `F6DFD04AD74F078DA70BE8F9FDECE26F07F9B4711882585EB1522F09A6AEC209`, `MATCH_MAIN=True MATCH_CHECKS=True`. Printed axioms of `readRatTag_of_tree` and `gcdBits_odd_pair` only `propext`, `Classical.choice`, `Quot.sound`; `gcdBits_mem_FP` / `readRatTag_mem_FP` / `decodeInputTag_empty` / `_none` / `_some` print the same trio. `ActualSelectedCmmsaSeededMap.lean` untouched (`blob 9fa1a0ed`, same as `91d8de7` / `38d9ad0`). Working-tree Main/Checks are later uncommitted Luna edits (`git diff --stat 93d3513` dirty on both Lean files); those edits are not part of this review. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze stop-loss: `gcdShared_eq` / `packReduced_eq` versus `ratTree`, then `readRatTag_of_tree`, then stop. Public surface that compiled: `decodeInputTag` (def), `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits` (def), `gcdBits_mem_FP`, `gcdBits_odd_pair`, `readRatTag` (def), `readRatTag_mem_FP`, `readRatTag_of_tree`. `decodeInputTag_mem_FP` is not declared; `selectedSeededMap` is not defined. `gcdBits_odd_pair` is the parent lemma, unchanged in body: canonical tapes, `a ≠ []`, and `b = [] ∨ odd a ∨ odd b` imply `gcdBits (pair a b) = (Nat.gcd (bitValue a) (bitValue b)).bits`. The new bridge is private: `shareTwos_canon` preserves canonicity along `share2Step`; `gcdShared_eq` runs `gcdPrep` then `gcdBits_odd_pair` on the `shareTwos_spec` remainder (empty-fst swap uses `gcdBits_of_fixed`; nonempty uses the odd/empty stuckness, which excludes leftover both-even); `packReduced_eq` (under `bitValue b ≠ 0`) rewrites `[true, true] ++ encodeDigits (quotBits fst gcd) ++ encodeDigits (quotBits snd gcd)` to `true :: encode (ratTree ((bitValue a : Rat) / bitValue b))` via `quotBits_eq`, `Nat.gcd_mul_right` for the shared `2^k`, and `rat_num_den_of_nat`. That encoding matches `Tree.encode (.node p q) = true :: encode p ++ encode q` and `natTree n = digitTree n.bits`. `readRatOnEncode_of_tree` cases `leaf` / unreadable digits / den=0 to `[]` against semantic `readRat`, and the den≠0 arm to `true :: encode (ratTree q)`. `readRatTag_of_tree` is the freeze statement: `treeParseTag (encode t) = true :: pair (encode t) []`, leftover-empty selects `readRatOnEncode`, then the private agreement. Checks `#check` / `#print axioms` the decodeInputTag names plus `gcdBits` / `gcdBits_mem_FP` / `gcdBits_odd_pair` / `readRatTag` / `readRatTag_mem_FP` / `readRatTag_of_tree`, inhabit `gcdBits ∈ FP` and `readRatTag ∈ FP`, reduce `readRatTag (encode leaf) = []` from `readRatTag_of_tree`, and reduce `decodeInputTag []` / truncated `decodeInputTag [true] = []`.

Hunts (absent). `sorry` on tree agreement: no `sorry` / `admit` / `native_decide` / new `axiom` in main or Checks at `93d3513`; the only `decide` uses are `Nat.div_lt_self` fuel and `0 < 2` / `2^1 ≤ 2^e` in private odd-part lemmas. Vacuous `readRatTag_of_tree`: the statement is `∀ t`, not a restriction to well-formed digit children; `leaf` and non-digit nodes take the `readRat = none` arm and pack `[]`. Dummy identity: `id_mem_FP` remains the Cobham stay-put / init API of `gcdStep` / `share2Step` / `gcdPrep`; `constFn_mem_FP []` is the parse-fail / leftover / den=0 branch; neither is `fpSeededMap` of `id`, `identitySeededMap`, nor a 3SAT→CMMSA compiler. Claiming `decodeInputTag_mem_FP` or Theorem 1: `decodeInputTag_mem_FP` is not a public name; `decodeInputTag_mem_FP_of_read` is still a private implication that needs `readInputTag ∈ FP`; Checks do not inhabit `decodeInputTag ∈ FP`. `gcdShared_eq` skipping both-even: `shareTwos_spec` proves the remainder is `a' = [] ∨ b' = [] ∨ odd a' ∨ odd b'` (the `k = |a|+|b|` leftover-even case is a length contradiction); `gcdPrep` only swaps empty fst. `packReduced` concat ≠ `ratTree` encode: `encodeDigits_eq` plus `Tree.encode` concat is the proved equality, not a shape comment. New axioms: none; printed set is the standard trio.

Notes: Checks coverage of `readRatTag_of_tree` is the `leaf → []` example only; there is no `rfl` on a concrete positive rational tree (`1/1`, unreduced `2/4`). `gcdBits_odd_pair` still excludes both-even pairs; that is an honest parent hypothesis, not a hole in `gcdShared_eq` / `readRatTag_of_tree` after `shareTwos`. `gcdShared_eq` is `Nat.gcd` of the *remaining* shareTwos parts, not of the original tapes; `packReduced_eq` restores the `2^k` factor with `Nat.gcd_mul_right`. `readRatTag_of_tree` is agreement on `encode t`, not a malformed-tape theorem; the definition still maps parse-fail and leftover rest to `[]`. Module header is the honest boundary (`readRatTag_of_tree` present; `decodeInputTag_mem_FP` remains). Unused `simp` arguments warn on main compile (`selectHead_true` / `selectHead_false` near `readRatOnEncode_of_tree` / `readRatTag_of_tree`) and do not change the theorems. `packReduced_eq` / `gcd_iterate_spec` raise `maxHeartbeats` to `800000`; that is a kernel budget, not a cheat. Main does not import `*Checks.lean`. Stopping after `readRatTag_of_tree` is the freeze-permitted stop-loss. Next consumer remains `readInputTag ∈ FP` then `decodeInputTag_mem_FP`; do not sorry those, and do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `decodeInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, P versus NP, publication.
