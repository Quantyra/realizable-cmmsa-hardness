# Actual SAT-to-3SAT source SeededMap proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `1488ff84c6d1daad1c1174aa469aeeddedc360aa` (`prove actual SAT-to-3SAT source SeededMap`). Main `5ADF857840ED4242C184AE248BCFC90703E8D1F250776A975C67B0A6E1A988E4`, Checks `16E3EF2C31E2BF2B1BDFE16D5C324793BBDC9553631FA22E9C7683CB4143B8E9`. Certification `b871a23a14a3b43f7ccbd150c2012a9fe0ed1e01`. Gate named `1488ff8` not `bba6dbb` (`certify.sh` rejects the sibling hash `bba6dbb380fa5dde490c45fd7806ccb05aa1e8a8`; `gate.sh` pins `EXPECTED_HEAD=1488ff84…`). 12 stages exit 0 (`exception-repair`, `formula`, `cmmsa-codec`, `randomized-reduction`, `randomized-reduction-assembly`, `actual-cmmsa-randomized-reduction`, `actual-theorem1`, `actual-headline-parameters`, main+Checks twice); objects `B96F861C31DE620329BB14C0926691815679B3E77386342CC5D3D1564C19E4BB` / `D9F1910626A7E431910440A18FC3CDF05815425BECF089CCAFEE06A5616B1435`. Independent working-tree SHA-256 of both Lean sources match the freeze and the gate. Cert commit adds evidence only; Lean blobs unchanged from `1488ff8`. Independent rehash `result=PASS`, 91 rows 0 mismatches. Forbidden scan clean. Printed axioms only `propext`, `Classical.choice`, `Quot.sound`. I made no Lean-source or Git changes and ran no additional broad build.

Freeze public statements compiled. `threeSatSource` is `ofLanguage ThreeSAT.language` (encoded satisfiable exact-3 CNFs), not a gap promise and not regularized 3-Lin. `satToThreeSatMap` is `fpSeededMap ThreeSAT.reduction reduction_mem_FP`: zero-coin `run = reduction ∘ pairFst`, `ruler = fun _ => []`, `coinCount = 0`. `satToThreeSatMap_apply` is `fpSeededMap_apply`; the seed is ignored by `pairFst_pair`, not by substituting `id`. `satToThreeSat_mapReducesVia` is the two `ofLanguage` sides of `z ∈ SAT ↔ reduction z ∈ 3SAT`: yes uses `(mem_language_iff_reduction_mem z).mp`; no uses `mt (… ).mpr` into the complement. `CNFSAT.language` is an `abbrev` of `SAT.language` (`language_eq_sat` is `rfl`), so the Completeness/Tseitin iff applies to the SAT yes/no sides. The underlying map is the total Tseitin transform (`CNF.decode?` then `to3Aux`, malformed → `fallbackEncoding` ∉ 3SAT), with `reduction_mem_FP` from the quartic TM. `satToThreeSat_preserves_zero` is `fpSeededMap_preserves` of that `MapReducesVia` (success probability `1` on both sides, then `1-0 ≤ 1`). `satToThreeSat_preserves` is `preserves_mono` of that `0 0` fact with four `norm_num`s (`0 ≤ 1/6`, `0 ≤ 1/6`, `1/6 ≤ 1`, `1/6 ≤ 1`), a slack lift of a deterministic Karp map, not a sampling construction. `satToThreeSat_exists` packages that map. `theorem1_from_threeSat_to_cmmsa` is `theorem1_headline` with `source = threeSatSource` and `hSatSrc = satToThreeSat_exists`; `hSrcCmmsa` remains a binder. Checks `#check` / `#print axioms` every public theorem, run `apply [] []`, and inhabit the SAT→3SAT `MapReducesVia` / `Preserves 0 0` / `Preserves (1/6)` / `exists` facts. Checks do not inhabit `theorem1_from_threeSat_to_cmmsa`.

Hunts (absent). Dummy identity as 3SAT-to-CMMSA: `identitySeededMap` is imported into scope via `ActualCMMSARandomizedReduction` and unused; it preserves `P` to `P` at error `0`, and `threeSatSource` is not definitionally `cmmsaPromise`. Claiming regularized 3-Lin: the source is `ThreeSAT.language`; `Finite3LinSource` is not referenced. Inhabiting `theorem1_from_threeSat_to_cmmsa`: no `example` or term supplies `hSrcCmmsa`; Checks stop at `#check`. Empty-source vacuity: both sides are total `ofLanguage` languages; the Karp no-side sends non-SAT strings, including malformed encodings, to `fallbackEncoding ∉ 3SAT`. No `sorry` / `admit` / `native_decide` / new `axiom`.

Notes: `Preserves (1/6)` is weaker than the native `0`-error Karp fact and does not introduce coins; consumers must not read it as a randomized 3SAT construction. Checks coverage of `apply` is the empty string only (`reduction []` is the fallback, a real equality). `theorem1_from_threeSat_to_cmmsa` still needs a 3SAT→`cmmsaPromise` `Preserves (1/6)` map and caller-supplied `1 ≤ sigmaL L`, `0 < gammaL L < 1`; it is not unconditional Theorem 1. `threeSatSource` NO is the full complement of encoded 3SAT, so a later source-to-CMMSA map must preserve malformed and non-3CNF strings as well as unsatisfiable 3CNFs. Module does not import `*Checks.lean`.

No HIGH vacuity, hypothesis, or statement bug. Not credited: regularized 3-Lin, unconditional Theorem 1, Corollary 2, inhabited `RandomizedPromiseNPHard (cmmsaPromise …)` without 3SAT→CMMSA `Preserves (1/6)`, P versus NP, publication.
