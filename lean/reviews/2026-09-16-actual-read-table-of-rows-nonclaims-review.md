# Actual readTableTag tree agreement: non-claims-boundary review

**GO-WITH-NOTES.**

Frozen source `a59171452a27edff5eaf99365fbcf64e402a307f` (`prove actual readTableTag tree agreement`). Gate/closeout name `a591714`. Main `ActualDecodeInputFP.lean` SHA-256 `7F1866ED2F508B2909E9D90C323BA5C4259969829ED0214E7967C0B3536367DE` (git blob `8ebb66f8f643bd3cd74c9c828c6d88aa728b33e9`, 391914 bytes, LF, git-object SHA-256 `AD262042E98262E9A48A7AE51506A6985C7CCE9ABF696F311A3948E01BAD2FD3`). Checks SHA-256 `278CD6A23C62792B75B62E670E1B44F256FCA45276FF6522CE005E94F465B621` (git blob `8046170dcdd7f8804864d79ab479de48e5516a78`, 5899 bytes, LF, git-object SHA-256 `A8500DAFE17D9AC1E75EF3CAA9B095A0E4419351B4276D9731970F1BD33E58F1`). Evidence `lean/evidence/2026-09-16-actual-read-table-of-rows-fresh-run/` and closeout `lean/evidence/2026-09-16-actual-read-table-of-rows-closeout.md`. Independently rehashed git blobs at `a591714`, certify commit `f5995f2`, and `HEAD` (Lean byte-identical) match the freeze and the closeout table. Certification commit `f5995f2` did not change the Lean sources. The working-tree Main/Checks are dirty (unpublished public `readInputTag` / `readInputTag_mem_FP` / `readInputTag_of_tree` / `decodeInputTag_mem_FP`, Checks inhabitants of `readInputTag ∈ Complexity.FP` and `decodeInputTag ∈ Complexity.FP`, plus CRLF) and are **not** the reviewed source. Recorded oleans `05288D3D0DDA3EFD6300642D10CE2774D4B77551DAFD010014D23880F97BEFE5` / `E615ED35D700F69C6C417E08391140702D4B6F0EDAD5B7B5B055A093D29F14E0` (local target-fresh twice, matching hashes, exit 0). Closeout SHA-256 `4B5259F0736B82956F37B2F12268837B1ED2C7E0AF18A6D4DE654BC2259E2DD4`. Local cert log SHA-256 `39A2E55E853717965C40EFF19205E2B0017B4E2659F1B97054909DFDD2566ED9`. GCP unavailability note SHA-256 `C564B438102DC1CCC1CFB55AB3CE8E345ACA0CC1FD9BCBF67660CA365FB616F7`. Parent Lean blob `a568f7140a45c00e807d7e461aeaf7f2344ac1b9` (`1b47db7`). `ActualSelectedCmmsaSeededMap.lean` is untouched (`1b47db7`, blob `9fa1a0ed2649fcb3424c62a861abd545f24b0357`). This lens did not compile or edit Lean. Review is of git blobs at `a591714` only.

Accepted, freeze stop-loss only (`readTableTag_of_rows`; packed `readTableTag` on `true :: encode (listTree (rows.map rowTree))` agrees with `FiniteSourceSampler.ValidRows`; no `decodeInput ∈ FP`):

- `readTableTag_of_rows` is the freeze statement: `readTableTag (true :: encode (listTree (rows.map rowTree)))` equals `true :: encode (listTree (rows.map rowTree))` when `ValidRows rows`, and `[]` otherwise. It is agreement on a well-formed packed row-list encoding, not `readTable ∈ FP`, not `readTableTag = pack ∘ readTable` on malformed/truncated tapes, not `readInputTag ∈ FP`, and not `decodeInputTag ∈ FP`.
- Private `validRowsFlag` remains private. Private `validRowsFlag_of_rows` agrees that flag with the three `ValidRows` conjuncts (`0 < rows.length`, nonnegative row probabilities, sum = 1) on the packed `validRun` / `foldAcc` accumulators. Empty list → `[]`. That private lemma is not a public theorem and is not `decodeInputTag_mem_FP`.
- `readTableTag_mem_FP` / `readTableTag_empty` / `listLenBits_mem_FP` / `listLenBits_of_listTree` remain packed Cobham memberships and parent tree facts from prior certified increments. They are not un-packed semantic `readTable ∈ FP` and are not this increment’s new public claim.
- Public packing of `decodeInput` exists as freeze-permitted progress: `decodeInputTag` / `_empty` / `_none` / `_some`. There is no public `decodeInputTag_mem_FP`, no `decodeInput ∈ FP`, and no Checks example inhabiting `decodeInputTag ∈ Complexity.FP`. `decodeInputTag_mem_FP_of_read` is a private conditional (`readInputTag ∈ FP → decodeInputTag ∈ FP`), not an unconditional membership. `readInputTag` remains private. There is no public `readInputTag_mem_FP`.
- `selectedSeededMap` is not defined. `hSrcCmmsa` is not inhabited. There is no dummy 3SAT→CMMSA identity. `ActualSelectedCmmsaSeededMap.lean` is untouched.

The module header denies `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, and P vs NP, and states that the module does not claim `decodeInputTag ∈ FP` until `decodeInputTag_mem_FP`. Checks `#check` / `#print axioms` `decodeInputTag`, `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits`, `gcdBits_mem_FP`, `gcdBits_odd_pair`, `readRatTag`, `readRatTag_mem_FP`, `readRatTag_of_tree`, `readSignedTag`, `readSignedTag_mem_FP`, `readSignedTag_of_tree`, `readListTag`, `readListTag_mem_FP`, `readListTag_of_tree`, `readFormulaTag`, `readFormulaTag_mem_FP`, `readFormulaTag_of_pair`, `readRowTag`, `readRowTag_mem_FP`, `readRowTag_of_pair`, `readRowListTag`, `readRowListTag_mem_FP`, `readRowListTag_of_pair`, `readParametersTag`, `readParametersTag_mem_FP`, `readParametersTag_of_tree`, `readTableTag`, `readTableTag_mem_FP`, `readTableTag_empty`, `readTableTag_of_rows`, `listLenBits`, `listLenBits_mem_FP`, `listLenBits_of_listTree`, and `listLenBits_leaf`; inhabit `gcdBits ∈ Complexity.FP`, `readRatTag ∈ Complexity.FP`, `readSignedTag ∈ Complexity.FP`, `readListTag ∈ Complexity.FP`, `readFormulaTag ∈ Complexity.FP`, `readRowTag ∈ Complexity.FP`, `readRowListTag ∈ Complexity.FP`, `readParametersTag ∈ Complexity.FP`, `readTableTag ∈ Complexity.FP`, and `listLenBits ∈ Complexity.FP`; reduce the empty packed-rows arm of `readTableTag_of_rows` to `[]`; and reduce `decodeInputTag [] = []` and truncated `decodeInputTag [true] = []`. Checks do **not** `#check` `decodeInputTag_mem_FP`, `readInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, or `hSrcCmmsa`, and do **not** inhabit `decodeInputTag ∈ Complexity.FP`. Main does not import `*Checks.lean`. `#print axioms readTableTag_of_rows` is `propext`, `Classical.choice`, `Quot.sound` only. Forbidden-scan of the freeze blobs is clean (`sorry` / `admit` / `native_decide` / new `axiom` absent; Theorem 1 / Corollary 2 / P vs NP appear only in denials). README, `INTEGRITY-CLAIMS.md`, `CHANGELOG.md`, and `CITATION.cff` are unchanged by this increment.

Notes, not blocking:

- The Main module docstring still frames packed helpers *toward* `ExecutablePipelineInput.decodeInput` and says `gcdBits` / `readRatTag` are in `FP`. That is the file-goal catalog line plus already-certified packed memberships, not a theorem that `decodeInput ∈ FP` or `decodeInputTag ∈ FP`. The Checks header and the closeout keep `decodeInputTag_mem_FP` open.
- The `readTableTag` docstring says pack `FiniteSourceSampler.readTable` on a packed row-list tape, with empty = none or `ValidRows` failure and nonempty = the same packed rows. That is the packing-contract line, not `readTable ∈ FP` and not agreement on malformed tapes. `readTableTag_of_rows` is only the well-formed `true :: encode (listTree …)` case.
- Checks phrasing that `readTableTag` / `_mem_FP` / `_of_rows` “pack `FiniteSourceSampler.readTable` / `ValidRows`” is the same packing-contract catalog. Do not cite it as un-packed `readTable ∈ FP`.
- Module/commit titles that say “actual readTableTag tree agreement” / `ActualDecodeInputFP` / Checks “packed decodeInput FP tag” are catalog labels. They name `readTableTag_of_rows` (and the already-certified packed `readTableTag ∈ FP`), not un-packed `readTable ∈ FP`, not `decodeInput ∈ FP`, not `selectedSeededMap`, not inhabited `hSrcCmmsa`, not unconditional Theorem 1, Corollary 2, publication, or `P` versus `NP`.
- Closeout phrasing “packed `readTableTag` on `true :: encode (listTree …)` agrees with `FiniteSourceSampler.ValidRows`” is the public theorem. Do not cite it as `decodeInputTag_mem_FP`. Closeout “Not `decodeInputTag_mem_FP`. Not Theorem 1.” keeps the forbidden classes closed.
- The Checks empty-list example inhabits the `ValidRows`-false arm of `readTableTag_of_rows` (`[]` is not nonempty). The `ValidRows`-true / keep-the-tape arm is in the theorem, not in a Checks numeric inhabitant of a nonempty valid table.
- Private `validRowsFlag_of_rows` / `validRows_iff` / `foldAcc_*` / `validRun_nonneg` / `validRun_neg` / `rowsEnc` support the public tree agreement. They are not additional public claims and are not `decodeInputTag_mem_FP`.
- The section heading “Packed cons-count of a list-tree encoding, then `readInput`” is parent catalog for `listLenBits`. It is not a public `readInputTag_mem_FP`.
- The truncated-`true` check is a runtime packing identity `decodeInputTag [true] = []`. It is not a named public FP theorem and is not `decodeInput ∈ FP`.
- Certification was local target-fresh of frozen `a591714` blobs after GCP `quantyra-lean-builder-01` reauthentication failure. The closeout records that limit. This lens does not treat local-vs-GCP as a claims expansion.
- A dirty working tree already adds public `readInputTag` / `readInputTag_mem_FP` / `readInputTag_of_tree` / `decodeInputTag_mem_FP` and Checks inhabitants of those FP memberships. That work is outside `a591714` and is not accepted by this review.
- Stopping after `readTableTag_of_rows` is the freeze-permitted stop-loss. It does not expand the claim boundary to `readInputTag_mem_FP`, `decodeInputTag_mem_FP`, or the rest of the SeededMap route.

Forbidden claims absent (`decodeInput ∈ FP`, Theorem 1, Corollary 2, P vs NP). The freeze’s `readInputTag_mem_FP` / `decodeInputTag_mem_FP` target remains open. Next: `readInputTag_mem_FP` from `readListTag` / `readRowListTag` / `readTableTag` / `readParametersTag` / `readNatTag` / `listLenBits`, then `decodeInputTag_mem_FP`. Then `selectedPairedRun_mem_FP` and `selectedSeededMap`. Then a 3SAT (or 3-Lin) → `encodeInput` table compiler ∈ FP with `MapReducesVia` into a selected pipeline promise. Do not inhabit `hSrcCmmsa` with identity, do not sorry `decodeInputTag_mem_FP`, and do not skip to Theorem 1.
