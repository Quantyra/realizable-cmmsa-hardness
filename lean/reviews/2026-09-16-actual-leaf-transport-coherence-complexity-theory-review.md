# Sequential-then-direct transport equality — complexity-theory review

**Verdict: GO-WITH-NOTES**

Frozen commit `8c2797867e81bd4f69c35893e3574a3f042d1132`. Main `10934636…EF5F`, Checks `A68C99D7…79BD`.

Sequential-then-direct equality is force-bearing path-independence for raw, side-condition-preserving transport among `Rel`-related presentations. Identity, inverse, and proof-irrelevance do not imply it: sequential data live on different two-way carriers, so a three-way common extension is required. Quantifiers include all `N,m,J,h` and the empty carrier. Checks call `exact transportedLabel_coherence` on a nonempty distinct triple with nonzero source RHS and on the empty carrier; they do not re-implement the theorem.

This is not representative-independent `LeafLabel` transport on `LeafVertex`. Named next consumers: presentation descent onto `LeafVertex`; repeated-address semantics. Not credited: sampler, star acceptance, hardness, reduction, `P = NP`, `P ≠ NP`, publication. Fixture note: nonempty Checks inhabit `h = 0`, `L = ⊥`.
