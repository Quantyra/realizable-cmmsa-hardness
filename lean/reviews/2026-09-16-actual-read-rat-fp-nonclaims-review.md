# Actual packed readRatTag FP tag: non-claims-boundary review

**GO-WITH-NOTES.**

Frozen source `dbdca8728e164f41d9fbe005ab699f48450c67f4`. Gate logs name `dbdca87`. Main `ActualDecodeInputFP.lean` SHA-256 `2508B3F45C37D04889D3B6448F0DAF44E8842F625B8D778D743E3A920D487763` (git blob `f1c8801e1e58edfa852904e018f0b762462926dd`, 95149 bytes). Checks SHA-256 `9380B508A6BF71A74311AA8B6884921982D9B8CB3CB0DDC4D43FE80DC65E0B7A` (git blob `77e607bf3b607fb7b278d731da08ca178acfbc47`, 1248 bytes). Evidence `research/evidence/2026-09-16-actual-read-rat-fp-fresh-run/` and closeout `research/evidence/2026-09-16-actual-read-rat-fp-closeout.md`. Independently rehashed local sources at `dbdca87`, certify commit `0a9b66e`, and `HEAD` (Lean byte-identical) match the freeze and the gate. Certification commit `0a9b66e` did not change the Lean sources. The working-tree Main is dirty (module-header rewrite plus CRLF) and is **not** the reviewed source; Checks working tree is byte-identical to the freeze. Independent rehash of the certification artifact table: PASS, 175 rows, 0 mismatches; `artifact-hashes.txt` SHA-256 `5CFAD7D15BCE0AF984309093A24971DC90DCF399B668BFD7642EEC026559BF5A`. Recorded objects `0F4729C564C61B7DF5B47091C59D826D3B13A7874553BEB0CDFD032D6EAC83DB` / `D22359AA6A705B5FB352C0A8573A5B1B113EEF59AC896DE0AC7369C3AB7A36C9`. Closeout SHA-256 `2C4DE8A2D930EEA05A7C69D6589F53887190C64736BA757FB79DB07695370BE1`.

Accepted, freeze stop-loss only (`readRatTag ∈ Complexity.FP`; total packed cobham composition on digit tapes; no `readRat ∈ FP`, no `decodeInput ∈ FP`):

- `readRatTag z` is `selectHead (emptyFlag (treeParseTag z)) [] (selectHead (emptyFlag (pairSnd (dropOne (treeParseTag z)))) (readRatOnEncode (pairFst (dropOne (treeParseTag z)))) [])`, a total `List Bool → List Bool`. Empty / malformed parse / trailing bits / den=0 go to `[]` by `selectHead` on `emptyFlag`. `readRatTag_mem_FP` places that packed function in `Complexity.FP` from Cobham composition of `treeParseTag_mem_FP`, `readRatOnEncode_mem_FP` (`packReduced` of `quotBits` / `gcdShared` / `shareTwos` / `stripTrailing` / `readDigitsTag`), and `selectHead` / `emptyFlag` / `const []`. It is not a semantic identity `readRatTag = pack ∘ readRat`, not `readRat ∈ FP`, not `readRatTag_of_tree`, and not `decodeInputTag ∈ FP`.
- `gcdBits_mem_FP` remains from the prior certified increment. It is still the packed `gcdStep` iterate, not `GCD ∈ FP` as a number-theoretic map.
- Public packing of `decodeInput` exists as freeze-permitted progress: `decodeInputTag` / `_empty` / `_none` / `_some`. There is no public `decodeInputTag_mem_FP`, no `decodeInput ∈ FP`, and no Checks example inhabiting `decodeInputTag ∈ Complexity.FP`. `decodeInputTag_mem_FP_of_read` is a private conditional (`readInputTag ∈ FP → decodeInputTag ∈ FP`), not an unconditional membership.
- `readRatTag_of_tree` is not defined. Private `readDigitsTag_of_tree` / `readNatTag_of_tree` and the `oddPart` / `gcdStep_iterate_oddPart` lemmas are supporting. They are not a public `gcdBits = encode ∘ gcd` and not `readRatTag = encode ∘ ratTree`.
- `selectedSeededMap` is not defined. `hSrcCmmsa` is not inhabited. There is no dummy 3SAT→CMMSA identity. `ActualSelectedCmmsaSeededMap.lean` is untouched (`38d9ad0`, blob `9fa1a0ed2649fcb3424c62a861abd545f24b0357`).

The module header denies `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, and P vs NP. Checks `#check` / `#print axioms` `decodeInputTag`, `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits`, `gcdBits_mem_FP`, `readRatTag`, and `readRatTag_mem_FP`; inhabit `gcdBits ∈ Complexity.FP` and `readRatTag ∈ Complexity.FP`; and reduce `decodeInputTag [] = []` and truncated `decodeInputTag [true] = []`. Checks do **not** `#check` `decodeInputTag_mem_FP`, `readRatTag_of_tree`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, or `hSrcCmmsa`. Main does not import `*Checks.lean`. `#print axioms readRatTag_mem_FP` is `propext`, `Classical.choice`, `Quot.sound` only. Forbidden-scan is clean. README, `INTEGRITY-CLAIMS.md`, `CHANGELOG.md`, and `CITATION.cff` are unchanged by this increment.

Notes, not blocking:

- The Main module docstring still says packed `ExecutablePipelineInput.decodeInput` is a total `List Bool → List Bool` function in `FP`. That is the file-goal catalog line, not a theorem. Do not cite the docstring as `decodeInput ∈ FP` or `decodeInputTag ∈ FP`. The Checks header and the closeout keep `decodeInputTag_mem_FP` open. A dirty working-tree rewrite of this header exists; it is not the frozen source.
- The `readRatTag` docstring says pack `readRat` as `true :: encode (ratTree q)`. That is the freeze packing-contract line, not a theorem. Do not cite it as `readRat ∈ FP` or as `readRatTag_of_tree`. The closeout keeps `readRatTag_of_tree` open.
- Module/commit titles that say "actual readRat packed FP tag" / `ActualDecodeInputFP` are catalog labels. They name `readRatTag ∈ Complexity.FP`, not the un-packed semantic `readRat`, not `decodeInput ∈ FP`, not `selectedSeededMap`, not inhabited `hSrcCmmsa`, not unconditional Theorem 1, Corollary 2, publication, or `P` versus `NP`.
- Private `readDigitsTag_mem_FP` / `readNatTag_mem_FP` / `quotBits_mem_FP` / `packReduced_mem_FP` / `gcdShared_mem_FP` / `shareTwos_mem_FP` are supporting Cobham lemmas. They are not additional public claims.
- The section heading "Semantic GCD of the subtractive/`dropTwos` iterate" is catalog text. Do not cite it as a public `Nat.gcd` identity.
- The truncated-`true` check is a runtime packing identity `decodeInputTag [true] = []`. It is not a named public FP theorem and is not `decodeInput ∈ FP`.
- Closeout phrasing "packed readRat in FP" is the packing `readRatTag`. Do not cite it as semantic `readRat` / `ratTree` agreement. The freeze’s `readRatTag_of_tree` obligation remains open.
- Stopping after `readRatTag_mem_FP` is the freeze-permitted stop-loss. It does not expand the claim boundary to `decodeInputTag`, or the rest of the SeededMap route.

Forbidden claims absent. The freeze’s `decodeInputTag_mem_FP` / `selectedPairedRun_mem_FP` / `selectedSeededMap` / `hSrcCmmsa` target remains open. Next: `readRatTag_of_tree`, then `decodeInputTag_mem_FP`. Then `selectedPairedRun_mem_FP` and `selectedSeededMap`. Then a 3SAT (or 3-Lin) → `encodeInput` table compiler ∈ FP with `MapReducesVia` into a selected pipeline promise. Do not inhabit `hSrcCmmsa` with identity, do not sorry `decodeInputTag_mem_FP`, and do not skip to Theorem 1.
