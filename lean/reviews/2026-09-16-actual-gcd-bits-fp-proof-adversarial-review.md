# Actual packed gcdBits FP tag proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `7105c9504c55226b4143624a4f0143379e11c1ba` (`prove actual gcdBits packed FP tag`). Main `8C9B46562B478EB93870249071E8636FFEC80A6F8A068E681D2467E0963FBD8A`, Checks `3FF960978E4C1567E74B764704612B72044A76639DA2D18EFAC0EF92921422E6`. Certification `7ccc14403273d2489dc89d3301e1cfeb9e01c449`. Gate named `7105c95` not `bba6dbb` (`certify.sh` rejects a sibling `bba6dbb` gate log; `gate.sh` pins `EXPECTED_HEAD=7105c950…`, `EXPECTED_MAIN=8C9B4656…`, `EXPECTED_CHECKS=3FF96097…`). 26 stages exit 0 (`exception-repair` through `actual-tree-parse-fp`, main×2, checks×2); objects `68D4E315CDC58A7410C0A5BB1C07ED5675F268040A26121BC735F24C52B749AD` / `4B90D9D4541098CE91846ECD6BA6945FB57A3122F28CF9F106593D01473315C8`. Independent `git cat-file` SHA-256 of both Lean blobs at `7105c95` match the freeze and the gate (`blob 7fff1264a3d6de0eb5bb5857ea4f638222992a27` / `bf6d15592bbe352aadd5f94971dc322bf699baa5`, 75230 / 1108 bytes). Cert commit `7ccc144` adds evidence only; Lean blobs unchanged from `7105c95` (`git diff --name-only 7105c95 7ccc144 -- '*.lean'` empty). Working-tree Checks SHA-256 matches; working-tree Main differs only by Windows `core.autocrlf` CRLF (`77029` bytes, SHA `E32904328D66C7582AEF567E21D5E4B8FAA588CD555579477A7F15845B0C6081`; porcelain clean). Independent rehash `result=PASS`, 175 rows 0 mismatches. Forbidden scan clean. Printed axioms of `gcdBits_mem_FP` only `propext`, `Classical.choice`, `Quot.sound`; `decodeInputTag_empty` / `_none` / `_some` print the same trio. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze stop-loss: packed binary GCD iterate in `FP`, then stop. Public surface that compiled: `decodeInputTag` (def), `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits` (def), `gcdBits_mem_FP`. `readRatTag` is omitted; `readRatTag_mem_FP` and `decodeInputTag_mem_FP` are not declared; `selectedSeededMap` is not defined. `gcdBits z` is `pairFst (gcdStep^[(gcdRuler z).length] z)` with ruler `z ++ z ++ replicate 16 false` of length `2|z|+16`. That definition is total on every `List Bool`, including empty, malformed pairing (`pairFst` / `pairSnd` default), and truncated digit tapes. `gcdStep` is a total bitstring step: stay on empty `pairFst`, empty `pairSnd`, or `eqFlag`; else branch on `ltCanon` to `pair a (dropTwos (subCanon b a))` or `pair (dropTwos (subCanon a b)) b`. `gcdBits_mem_FP` is Cobham `iterate_mem_FP` of that step, init `id`, ruler `gcdRuler`, width `gcdWidth` of length `(2|z|+16)^2`, then `mem_FP_comp` with `fstBlock`. The iterate bound is `∀ z n, (gcdStep^[n] z).length ≤ |gcdWidth z|` via `GcdReach` (`fst`/`snd` stay `≤ |z|`), not a restriction to well-formed pairs. Canonical subtractive length is `subCanon_length_of_le`: under `bitValue b ≤ bitValue a`, `subCanon a b = (bitValue a - bitValue b).bits` from `subCanon_bitValue` plus `stripTrailing_eq_bits`, hence `|subCanon a b| ≤ |a|`. That closes the freeze blocker (`|a|+|b|+1` not iterable). Checks `#check` / `#print axioms` the decodeInputTag names plus `gcdBits` / `gcdBits_mem_FP`, inhabit `gcdBits ∈ FP`, and reduce `decodeInputTag []` / truncated `decodeInputTag [true] = []`.

Hunts (absent). `sorry` on gcd FP: no `sorry` / `admit` / `native_decide` / new `axiom` in main or Checks. `gcdBits` not total: the def is a total `List Bool → List Bool`; empty and malformed pair projections still iterate `gcdStep` and return `pairFst`. Iterate width not polynomial: `gcdWidth` is quadratic in `|z|` and in `FP` via `mulLenFn`; `hbound` is exactly `iterate_mem_FP`'s hypothesis on every prefix of the init trajectory, using `subCanon_length_of_le` rather than the loose `|a|+|b|+1` bound. Claiming `readRatTag_mem_FP` or `decodeInputTag_mem_FP`: neither name is declared; `decodeInputTag_mem_FP_of_read` is a private implication that still needs `readInputTag ∈ FP`; Checks do not inhabit `decodeInputTag ∈ FP`. Dummy identity: `id_mem_FP` is the Cobham init and the stay-put branch of `gcdStep`; it is not `fpSeededMap` of `id`, not `identitySeededMap`, and not a 3SAT→CMMSA compiler. New axioms: none; printed set is the standard trio.

Notes: Checks coverage of `gcdBits` is membership only; there is no `rfl` example on a concrete pair, and no `bitValue (gcdBits (pair a b)) = Nat.gcd …` theorem. `gcdBits` is the truncated Stein-like iterate, not a proved `Nat.gcd` packing: `dropTwos` is applied only to the difference, common factors of two are not accumulated, and empty `pairFst` stay-put yields `[]` rather than the other tape. Ruler `2|z|+16` is polynomial and used; it is not proved to reach a stuck GCD state. Module header still describes packed `decodeInput` in `FP`; Checks header is the honest boundary (`gcdBits_mem_FP` on the Cobham surface; `readRatTag_mem_FP` / `decodeInputTag_mem_FP` remain). Unused `simp` arguments warn on main compile and do not change the theorems. `subCanon_bitValue` is private. Main does not import `*Checks.lean`. Stopping after `gcdBits_mem_FP` is the freeze-permitted stop-loss. Next consumer remains `readRatTag_mem_FP` (semantic GCD of the iterate, then reduced `ratTree`), then `decodeInputTag_mem_FP`; do not sorry those, and do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `readRatTag_mem_FP`, `decodeInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, P versus NP, publication.
