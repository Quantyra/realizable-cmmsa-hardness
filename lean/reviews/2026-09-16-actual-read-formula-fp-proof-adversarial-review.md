# Actual packed readFormula FP tag proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `41c6e1ca1d4fc3d264d319fdd20cb65170b1851c` (`prove actual readFormula packed FP tag`). Parent `8aad0e1` (`prove actual formStep_encode`). Main SHA-256 `ACD77A39B055A6F0630661D86D785BF256AE03B2FBDA15B682706373C9B09821`, Checks `9200399532E217768D248356E9FDDC2E4346D4C3C5DB9A72BC1E5BB2C3025A60`. Certification `0210ae9fc35c5689ff738c5ad09b71f452eb3031` (`certify actual readFormula packed FP tag (local after GCP auth fail)`). GCP `quantyra-lean-builder-01` unavailable (`Reauthentication failed`); local target-fresh of frozen blobs twice, main+Checks exit 0, objects `C118C1F7DDCD60180FB143CD571AD763F7318AC0E7B5AB9A9549AC7D6AABFA6E` / `12273BFE4B9EA38F8116DB27AB611B6079DFF87D8CB3DEF2EFD86EFC6480D048`, `MATCH_MAIN=True MATCH_CHECKS=True`. Independent `git cat-file` SHA-256 of both Lean blobs at `41c6e1c` match the freeze and closeout (`blob ac96f44770354d3ab7a8799c4fa07d438ab5877e` / `7d55f944c6432b1e7b5e00cc5fbda7ca9a5ec7f0`, 264952 / 2680 bytes, LF-only). `git hash-object` of those extracted blobs reproduces the same ids. Cert commit `0210ae9` adds evidence only; Lean blobs unchanged from `41c6e1c` (`git diff --name-only 41c6e1c HEAD -- '*.lean'` empty). Printed axioms of `readFormulaTag_mem_FP` only `propext`, `Classical.choice`, `Quot.sound`; `gcdBits_mem_FP` / `readRatTag_mem_FP` / `readSignedTag_mem_FP` / `readListTag_mem_FP` / `decodeInputTag_empty` / `_none` / `_some` print the same trio. `ActualSelectedCmmsaSeededMap.lean` untouched (`blob 9fa1a0ed`, same as `8aad0e1` / `38d9ad0`). Working-tree Main/Checks are later uncommitted Luna edits (`git diff --stat 41c6e1c` dirty on both Lean files, including a post-freeze `readFormulaTag_of_pair`); those edits are not part of this review. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze stop-loss: `formReach_step` using `formula_encode_le` on the parent, then `formReach_iterate` / `formStep_iterate_length` / `Cobham.iterate_mem_FP`, then `readFormulaTag_mem_FP`, then stop. Public surface that compiled: `decodeInputTag` (def), `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits` (def), `gcdBits_mem_FP`, `gcdBits_odd_pair`, `readRatTag` / `_mem_FP` / `_of_tree`, `readSignedTag` / `_mem_FP` / `_of_tree`, `readListTag` / `_mem_FP` / `_of_tree`, `readFormulaTag` (def), `readFormulaTag_mem_FP`. `readFormulaTag_of_pair` is not declared; `decodeInputTag_mem_FP` is not declared; `selectedSeededMap` is not defined. Parent `8aad0e1` left `FormReach` / `formReach_step` / `readFormulaTag_mem_FP` in a comment (`Reach and readFormulaTag_mem_FP deferred until formReach_step uses formula_encode_le on the parent formula, not |left|+|right|`). This commit uncomments that block, adds `framePayload` / `stackPayload`, tightens `encodeFormMode_len_exact`, adds `FormReach.measure_le` (`formMeasure s ≤ 2|z|+2`) with `formMeasure_step_le` / `stack_length_le_stackWork`, and closes cubic `encodeFormSem_length_le` against `formWidth`. `readFormulaTag z` is `formPack (formStep^[(formRuler z).length] (formInit z))` with ruler `z ++ z ++ [false, false]` of length `2|z|+2` and width `(2|z|+32)^3`. That definition is total on every `List Bool`, including empty, parse-fail, leftover rest (`initForm` → fail), and truncated pair projections. `formStep` is a total bitstring expand/reduce machine: stay on fail (`flag = []`) and success (`flag = [true, true]`); else `formExpand` / `formReduce`. `formStep_encode` (parent) agrees with `formSemStep` on every encoded `FormSem`. Combine reduces wrap `leftEnc`/`enc` as `formulaTree (.and/.or fl fq)` via `wrapAndEnc_eq` / `wrapOrEnc_eq`; `formula_encode_le` then gives `|encode (formulaTree f)| ≤ 8|encode parent|+8`, and `stackOk` has `|encode parent| ≤ |z|`, so the new reduce tape stays `≤ 8|z|+8`. That is the freeze blocker: `|left|+|right|+7` on two `8|z|+8` children would be `16|z|+23` and would break `reduce_le`. `readFormulaTag_mem_FP` is Cobham `iterate_mem_FP` of that step, init `formInit`, ruler `formRuler`, width `formWidth`, then `mem_FP_comp` with `formPack`. The iterate bound is `∀ z n, (formStep^[n] (formInit z)).length ≤ |formWidth z|` via `FormReach` on every prefix of the init trajectory (`formReach_init`, `formReach_step`, `formReach_iterate`), not a restriction to well-formed `pair n.bits (encode t)`. Checks `#check` / `#print axioms` the decodeInputTag names plus the gcd/rat/signed/list surface plus `readFormulaTag` / `readFormulaTag_mem_FP`, inhabit `readFormulaTag ∈ FP` (and the prior packed tags), and reduce `decodeInputTag []` / truncated `decodeInputTag [true] = []`.

Hunts (absent). `sorry` on formula FP: no `sorry` / `admit` / `native_decide` / new `axiom` in main or Checks at `41c6e1c`; the only `decide` uses are prior `Nat.div_lt_self` fuel and `0 < 2` / `2^1 ≤ 2^e` in private odd-part lemmas. Claiming `decodeInputTag ∈ FP`: `decodeInputTag_mem_FP` is not a public name; `decodeInputTag_mem_FP_of_read` is still a private implication that needs `readInputTag ∈ FP`; Checks do not inhabit `decodeInputTag ∈ FP`; module and Checks headers say the name remains. Dummy identity: `id_mem_FP` is the Cobham stay-put branch of `formStep` on fail/success (and prior gcd/share/list stay-put); `constFn_mem_FP []` is the `formPack` fail/expand arm; neither is `fpSeededMap` of `id`, `identitySeededMap`, nor a 3SAT→CMMSA compiler. `formStep` is not the identity function: expand pushes `waitRight` and walks children; reduce wraps `formulaTree` encodings. Width from `|left|+|right|`: combine uses `formula_encode_le` on `parent` with `readFormula bound parent = some (and/or fl fq)`, not `wrapAndEnc_length` / `wrapOrEnc_length` sums. New axioms: none; printed set is the standard trio. Theorem 1 / `hSrcCmmsa`: not inhabited; `ActualSelectedCmmsaSeededMap.lean` is untouched.

Notes: Checks coverage of `readFormulaTag` is membership only; there is no `rfl` on a concrete formula tree. `readFormulaTag_of_pair` is not declared at this freeze (a later dirty working-tree block of that name is out of scope). The `readFormulaTag` docstring states packing `readFormula (bitValue (pairFst z))` on a complete `pairSnd` tree as intent; `pack_evalForm` / `evalForm_iterate` / `iterate_ge_formStuck` / `FormReach.measure_le` are present and the ruler length equals the measure cap, but they are not assembled into a public agreement theorem. `formWidth` is cubic in `|formArg| = 2|z|+32` and in `FP` via `mulLenFn`; that is polynomial, not a stuckness theorem. Module header is the honest boundary (`readFormulaTag_mem_FP` present; `decodeInputTag_mem_FP` remains). `maxHeartbeats` raises on prior gcd/quot proofs are kernel budget, not a cheat. Main does not import `*Checks.lean`. Stopping after `readFormulaTag_mem_FP` is the freeze-permitted stop-loss (`of_pair` was optional-if-green). Next consumer remains `readFormulaTag_of_pair`, then `readInputTag_mem_FP` / `decodeInputTag_mem_FP`; do not sorry those, and do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `readFormulaTag_of_pair`, `decodeInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, P versus NP, publication.
