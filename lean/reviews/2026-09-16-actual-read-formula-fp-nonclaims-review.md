# Actual packed readFormulaTag FP tag: non-claims-boundary review

**GO-WITH-NOTES.**

Frozen source `41c6e1ca1d4fc3d264d319fdd20cb65170b1851c` (`prove actual readFormula packed FP tag`). Gate/closeout name `41c6e1c`. Main `ActualDecodeInputFP.lean` SHA-256 `ACD77A39B055A6F0630661D86D785BF256AE03B2FBDA15B682706373C9B09821` (git blob `ac96f44770354d3ab7a8799c4fa07d438ab5877e`, 264952 bytes, LF, git-object SHA-256 `B02DECD6794F6F1EE666213326A07287A5C344A7BCA0222B334DC51B1331DA61`). Checks SHA-256 `9200399532E217768D248356E9FDDC2E4346D4C3C5DB9A72BC1E5BB2C3025A60` (git blob `7d55f944c6432b1e7b5e00cc5fbda7ca9a5ec7f0`, 2680 bytes, LF, git-object SHA-256 `A49782BD899C73B071A19CB75A9E67E6A638A198DD57F889756EF34DD13899AC`). Evidence `research/evidence/2026-09-16-actual-read-formula-fp-fresh-run/` and closeout `research/evidence/2026-09-16-actual-read-formula-fp-closeout.md`. Independently rehashed git blobs at `41c6e1c`, certify commit `0210ae9`, and `HEAD` (Lean byte-identical) match the freeze and the closeout table. Certification commit `0210ae9` did not change the Lean sources. The working-tree Main/Checks are dirty (unpublished `readFormulaTag_of_pair` plus Main CRLF) and are **not** the reviewed source. Recorded oleans `C118C1F7DDCD60180FB143CD571AD763F7318AC0E7B5AB9A9549AC7D6AABFA6E` / `12273BFE4B9EA38F8116DB27AB611B6079DFF87D8CB3DEF2EFD86EFC6480D048` (local target-fresh twice, matching hashes, exit 0). Closeout SHA-256 `EA648E342F0DDA2EA29F4E249338EBB1FA11004B3E3BBDC78564629D7B827033`. Local cert log SHA-256 `5CBBD86B8ECCBA92DD604FAE4A61D43801CC2279165E6D6EE4D62372C7C27849`. Axiom log SHA-256 `BC44D5307F150E0341DB76F54D151FFD637DA67785E394F493E71242E31FC69B`. GCP unavailability note SHA-256 `0E3CB35D234A51CEE8FCB77B0F33B8EC3472240CAD9A70370530B78BF8738A08`. Parent Lean blob `e74a0127efa37cb5f152bc78eba90855ec99cb75` (`8aad0e1`). `ActualSelectedCmmsaSeededMap.lean` is untouched (`38d9ad0`, blob `9fa1a0ed2649fcb3424c62a861abd545f24b0357`). This lens did not compile or edit Lean.

Accepted, freeze stop-loss only (`readFormulaTag ∈ Complexity.FP`; total packed cobham iterate of `formStep`; no `readFormula ∈ FP`, no `decodeInput ∈ FP`):

- `readFormulaTag z` is `formPack (formStep^[(formRuler z).length] (formInit z))`, a total `List Bool → List Bool`. Empty / malformed parse / trailing bits / fail mode go to `[]` by `formPack` on `formFlag`. `readFormulaTag_mem_FP` places that packed function in `Complexity.FP` from Cobham `iterate_mem_FP` of private `formStep` with polynomial `formRuler` / `formWidth`, using private `formReach_step` / `formReach_iterate` / `formStep_iterate_length` and parent `formStep_encode`, then `formPack`. It is not a semantic identity `readFormulaTag = pack ∘ readFormula`, not `readFormula ∈ FP`, not `readFormulaTag_of_pair`, and not `decodeInputTag ∈ FP`.
- Private `formStep_encode` is bit-step / semantic-stepper agreement (`formStep (encodeFormSem s) = encodeFormSem (formSemStep s)`). Private `formula_encode_le` bounds `encode (formulaTree f)` by the parent tree, not `|left|+|right|`. They support the iterate width. They are not public theorems and are not `readFormulaTag_of_pair`.
- `gcdBits_mem_FP` / `readRatTag_mem_FP` / `readSignedTag_mem_FP` / `readListTag_mem_FP` and the corresponding `_of_tree` / `gcdBits_odd_pair` theorems remain from prior certified increments. They are not un-packed semantic readers of `readFormula`.
- Public packing of `decodeInput` exists as freeze-permitted progress: `decodeInputTag` / `_empty` / `_none` / `_some`. There is no public `decodeInputTag_mem_FP`, no `decodeInput ∈ FP`, and no Checks example inhabiting `decodeInputTag ∈ Complexity.FP`. `decodeInputTag_mem_FP_of_read` is a private conditional (`readInputTag ∈ FP → decodeInputTag ∈ FP`), not an unconditional membership.
- `readFormulaTag_of_pair` is not defined on the freeze blobs. `readRow` / `readTable` / `readParameters` are absent. `readInputTag` remains private.
- `selectedSeededMap` is not defined. `hSrcCmmsa` is not inhabited. There is no dummy 3SAT→CMMSA identity. `ActualSelectedCmmsaSeededMap.lean` is untouched.

The module header denies `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, and P vs NP, and states that the module does not claim `decodeInputTag ∈ FP` until `decodeInputTag_mem_FP`. Checks `#check` / `#print axioms` `decodeInputTag`, `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits`, `gcdBits_mem_FP`, `gcdBits_odd_pair`, `readRatTag`, `readRatTag_mem_FP`, `readRatTag_of_tree`, `readSignedTag`, `readSignedTag_mem_FP`, `readSignedTag_of_tree`, `readListTag`, `readListTag_mem_FP`, `readListTag_of_tree`, `readFormulaTag`, and `readFormulaTag_mem_FP`; inhabit `gcdBits ∈ Complexity.FP`, `readRatTag ∈ Complexity.FP`, `readSignedTag ∈ Complexity.FP`, `readListTag ∈ Complexity.FP`, and `readFormulaTag ∈ Complexity.FP`; reduce the prior leaf `_of_tree` examples; and reduce `decodeInputTag [] = []` and truncated `decodeInputTag [true] = []`. Checks do **not** `#check` `readFormulaTag_of_pair`, `decodeInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, or `hSrcCmmsa`, and do **not** inhabit `decodeInputTag ∈ Complexity.FP`. Main does not import `*Checks.lean`. `#print axioms readFormulaTag_mem_FP` is `propext`, `Classical.choice`, `Quot.sound` only. Forbidden-scan of the freeze blobs is clean (`sorry` / `admit` / `native_decide` / new `axiom` absent; Theorem 1 / Corollary 2 / P vs NP appear only in denials). README, `INTEGRITY-CLAIMS.md`, `CHANGELOG.md`, and `CITATION.cff` are unchanged by this increment.

Notes, not blocking:

- The Main module docstring still frames packed helpers *toward* `ExecutablePipelineInput.decodeInput` and says `gcdBits` / `readRatTag` are in `FP`. That is the file-goal catalog line plus already-certified packed memberships, not a theorem that `decodeInput ∈ FP` or `decodeInputTag ∈ FP`. The Checks header and the closeout keep `decodeInputTag_mem_FP` open.
- The `readFormulaTag` docstring says pack `readFormula (bitValue (pairFst z))` on the complete tree encoding `pairSnd z`, with empty = none and nonempty = `true :: encode (formulaTree f)`. That is the freeze packing-contract line, not a theorem. Do not cite it as `readFormula ∈ FP` or as `readFormulaTag_of_pair`. The closeout keeps `readFormulaTag_of_pair` open.
- Header phrasing “the formula bit-step agrees with the semantic stepper (`formStep_encode`)” names the private parent lemma. It is not a public semantic identity for `readFormulaTag` and is not `readFormulaTag_of_pair`.
- Module/commit titles that say “actual readFormula packed FP tag” / `ActualDecodeInputFP` / Checks “packed decodeInput FP tag” are catalog labels. They name `readFormulaTag ∈ Complexity.FP`, not the un-packed semantic `readFormula`, not `decodeInput ∈ FP`, not `selectedSeededMap`, not inhabited `hSrcCmmsa`, not unconditional Theorem 1, Corollary 2, publication, or `P` versus `NP`.
- Closeout phrasing “packed readFormula in FP” is the packing `readFormulaTag`. Do not cite it as semantic `readFormula` / `formulaTree` agreement. The freeze’s optional `readFormulaTag_of_pair` obligation remains open.
- Private `formReach_step` / `formReach_iterate` / `formStep_iterate_length` / `formPack_mem_FP` / `formInit_mem_FP` / `formMeasure_step_le` are supporting Cobham / reach lemmas. They are not additional public claims.
- The truncated-`true` check is a runtime packing identity `decodeInputTag [true] = []`. It is not a named public FP theorem and is not `decodeInput ∈ FP`.
- Certification was local target-fresh of frozen `41c6e1c` blobs after GCP `quantyra-lean-builder-01` reauthentication failure. The closeout records that limit. This lens does not treat local-vs-GCP as a claims expansion.
- A dirty working tree already adds `readFormulaTag_of_pair`. That work is outside `41c6e1c` and is not accepted by this review.
- Stopping after `readFormulaTag_mem_FP` is the freeze-permitted stop-loss. It does not expand the claim boundary to `readFormulaTag_of_pair`, `decodeInputTag`, or the rest of the SeededMap route.

Forbidden claims absent (`decodeInput ∈ FP`, Theorem 1, Corollary 2, P vs NP). The freeze’s `readFormulaTag_of_pair` / `readRow` / `readTable` / `readParameters` / `readInputTag_mem_FP` / `decodeInputTag_mem_FP` target remains open. Next: `readFormulaTag_of_pair`, then `decodeInputTag_mem_FP` via `readInputTag ∈ FP`. Then `selectedPairedRun_mem_FP` and `selectedSeededMap`. Then a 3SAT (or 3-Lin) → `encodeInput` table compiler ∈ FP with `MapReducesVia` into a selected pipeline promise. Do not inhabit `hSrcCmmsa` with identity, do not sorry `decodeInputTag_mem_FP`, and do not skip to Theorem 1.
