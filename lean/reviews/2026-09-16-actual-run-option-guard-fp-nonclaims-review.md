# Actual packed runOptionGuardTag FP tag: non-claims-boundary review

**GO-WITH-NOTES.**

Frozen source `c78bf1a5b4c8c31fe82535f3f293c57c2b348da2` (`prove actual runOptionGuard packed FP tag`). Gate/closeout name `c78bf1a`. Main `ActualSelectedCmmsaSeededMap.lean` SHA-256 `99B5245219A98031C9BA4537E19E2968FF29AD0327F41153CBF8B3275D5EB152` (git blob `7fe2f4c6bf7faffee4ea20d70b0cb108f57941a6`, 35975 bytes, LF, git-object SHA-256 `17EEC451530DB1EA822F093585252A9016E9DAC4075894BDC6BE386EEF3E3AD7`). Checks SHA-256 `FAF432B65CAC722C809646E2979EB8F2CEB43037D7018944C35DD64A055F38D8` (git blob `373bbdbd75f65f21c7b48867d973c600ea0dc224`, 2183 bytes, LF, git-object SHA-256 `62D903219F3547CE0852C2D70DA9047465C5F7A77A79B7E3F322E75FFF04D730`). Evidence `lean/evidence/2026-09-16-actual-run-option-guard-fp-fresh-run/` and closeout `lean/evidence/2026-09-16-actual-run-option-guard-fp-closeout.md`. Independently `git cat-file`’d and rehashed those blobs at `c78bf1a`; recomputed git-ids of the extracted bytes reproduce the stored blob ids. Certify commit `01e5991` (`01e599122f2a345e3c1cd4ab3bde3f35ef4d8d58`), `HEAD` (Lean byte-identical), and the working-tree Lean match the freeze and the closeout table. Certification commit `01e5991` is evidence-only (`git diff --name-only c78bf1a 01e5991 -- '*.lean'` empty; same Main/Checks blob ids) and did not change the Lean sources. Recorded oleans `960B9AB41DA7C6BF5621A2BB285FA926A1D43F34AEE63E2E5AF7352F7BFB1651` / `6C841EBD06E28B9E470E600F26517364C4A270E49083B5A1F8B630BCC8CD4B9B` (local target-fresh twice after GCP idle-stop, matching hashes, exit 0). Closeout SHA-256 `25FB6706B5E5D8C263BDDAF60B90C5D6CC17D2F7536C99BF9C310777BEEBE9AF` (git blob `7c4070a0dc10a2afca6feedf1e9cf3b8c2094d27`, 1555 bytes, LF). Local cert log git-blob SHA-256 `F73488C4A2B956DF7F601A2F77FDB92BF4A13C9C9F572EF084BBA807EFAC3DE1` (blob `efee833ac1a6b49ad560522386cbe28c799edf4e`, 3169 bytes, LF). GCP unavailability note SHA-256 `6883DCE49FE60603F475EAA02807354CCA47CE6F8E4F9B813252276390615A7D` (git blob `626fa85db99019cc86acf62e419658ef47a08f5e`). Parent Lean blob `de09577c74a2910aa2b81a360a5b4c3b5272878f` (`b8ba7b5` / `cba404e` / `374e1ba`, SHA-256 `1B3BCB0452CD11DCB9C7B61E97F35FF8807297C0526F92534BF9DD2F3D7E69C9`). `MANUSCRIPT.md` unpublished bytes are unchanged (`9ec48580b55b3e34afad115199817da3c1b06eb3`, SHA-256 `FF00997C8C243E88982C41B0B0E24FAAAAFE36CEC6F1903824599DC242538686`). README, `CITATION.cff`, `lean/README.md`, `lean/PROVENANCE.md`, `lakefile.toml`, and `lean/PvNP.lean` are unchanged by this increment. `ActualDecodeInputFP.lean` is untouched (`aba9e488124928fba7d18fbb22f6d056b20c8eff`). `ExecutablePipelineInput.lean` / `ExecutableSamplingPolicy.lean` blobs `f4593745` / `0a2583db` unchanged. This lens did not compile or edit Lean. Review is of git blobs at `c78bf1a` only.

Accepted, freeze stop-loss only (packed `runOptionGuardTag ∈ Complexity.FP` with decode + unary `trials*precision` length guard; no `runOptionTag_mem_FP`; no `selectedPairedRun_mem_FP`):

- `runOptionGuardTag z` is a total `List Bool → List Bool` packing of the decode + unary `trials*precision` length guard of `runOption`. Failure (`decodeInput (pairFst z) = none`, or coin-tape length ≠ packed `trialsPrecisionRuler z`) is `[]`. Success is `true :: pair (pairFst z) (pairSnd z)`. It does not execute `checkedBits` / `accepted`, does not emit a run output tree, and does not pack `runOption` itself. `runOptionGuardTag_mem_FP` places that packed function in `Complexity.FP` from Cobham composition of parent `decodeInputTag_mem_FP` / `instTag_mem_FP`, `emptyFlag`, `lenEqFlag`, `selectHead`, `pair`, and the private unary `precision`/`trials` ruler. It is not un-packed `runOption ∈ FP` and not `selectedPairedRun_mem_FP`.
- `runOptionGuardTag_none` is the decode-failure packing identity: if `decodeInput (pairFst z) = none` then `runOptionGuardTag z = []`. It is a packing-contract lemma on the fail arm, not `decodeInput ∈ FP` and not `runOption ∈ FP`.
- `runOptionGuardTag_empty` is the empty-tape packing identity `runOptionGuardTag [] = []`, via `runOptionGuardTag_none` and `decodeInput_empty`. It is not a named public theorem that `runOption ∈ FP`.
- There is no public `runOptionGuardTag_eq` and no public `runOptionTag_mem_FP`. Parent `paddedRunGuardTag_mem_FP` / `_eq` / `_empty` and `selectedCoinRuler_mem_FP` remain prior packed theorems. Parent `selectedPairedRun L eps` remains the `paddedRun` wrapper on `pairFst` / `pairSnd`. There is still no `selectedPairedRun_mem_FP` and no `paddedRun_mem_FP`. Empty-tape `selectedPairedRun L (1/4) [] = []` remains in Checks.
- `selectedSeededMap` is not defined. `hSrcCmmsa` is not inhabited. There is no dummy 3SAT→CMMSA identity.

The module header denies `selectedPairedRun_mem_FP` (`checkedBits` / `accepted` / output `tree` are not on the Cobham FP surface), `selectedSeededMap`, inhabited `hSrcCmmsa`, dummy 3SAT→CMMSA identity, unconditional Theorem 1, Corollary 2, and P vs NP. Checks `#check` / `#print axioms` `selectedPairedRun`, `selectedCoinRuler`, `selectedCoinRuler_mem_FP`, `paddedRunGuardTag`, `paddedRunGuardTag_mem_FP`, `paddedRunGuardTag_eq`, `paddedRunGuardTag_empty`, `runOptionGuardTag`, `runOptionGuardTag_mem_FP`, `runOptionGuardTag_none`, and `runOptionGuardTag_empty`; inhabit `selectedCoinRuler eps ∈ Complexity.FP`, `paddedRunGuardTag eps ∈ Complexity.FP`, and `runOptionGuardTag ∈ Complexity.FP` via `example : runOptionGuardTag ∈ Complexity.FP := runOptionGuardTag_mem_FP`; reduce ruler length to `coinRuler`; reduce empty-tape `selectedPairedRun L (1/4) [] = []`; reduce `paddedRunGuardTag eps [] = []`; and reduce `runOptionGuardTag [] = []` via `runOptionGuardTag_empty`. Checks do **not** `#check` `selectedPairedRun_mem_FP`, `runOptionTag_mem_FP`, `selectedSeededMap`, or `hSrcCmmsa`, and do **not** inhabit un-packed `runOption ∈ Complexity.FP` or `selectedPairedRun ∈ Complexity.FP`. Main does not import `*Checks.lean`. `#print axioms runOptionGuardTag_mem_FP` / `runOptionGuardTag_none` / `runOptionGuardTag_empty` are `propext`, `Classical.choice`, `Quot.sound` only. Forbidden-scan of the freeze blobs is clean (`sorry` / `admit` / `native_decide` / new `axiom` absent; Theorem 1 / Corollary 2 / P vs NP / `selectedSeededMap` / `hSrcCmmsa` / dummy 3SAT identity appear only in denials). README, `CITATION.cff`, `lean/README.md`, `lean/PROVENANCE.md`, and `MANUSCRIPT.md` are unchanged by this increment.

Notes, not blocking:

- The Main module docstring frames the packed decode/`trials*precision` length guard of `runOption` and still says `selectedPairedRun_mem_FP` is omitted. That is the file-goal catalog plus this increment’s packed membership, not a theorem that `runOption ∈ FP` is Theorem 1 material.
- The `runOptionGuardTag` docstring says success payload is `true :: pair instanceBits coins` and failure is `[]`. That is the packing-contract line. The payload is the input pair, not a `runOption` / `checkedBits` output.
- Module/commit titles that say “actual runOptionGuard packed FP tag” / Checks “packed decode/`trials*precision` length guard of `runOption`” are catalog labels. They name packed `runOptionGuardTag ∈ Complexity.FP`, not `runOptionTag_mem_FP`, not `selectedPairedRun_mem_FP`, not `selectedSeededMap`, not inhabited `hSrcCmmsa`, not unconditional Theorem 1, Corollary 2, publication, or `P` versus `NP`.
- Closeout phrasing “packed decode + unary `trials*precision` length guard of `runOption`” is `runOptionGuardTag_mem_FP`. Closeout “Not `runOptionTag_mem_FP`. Not `selectedPairedRun_mem_FP`. Not Theorem 1.” keeps the forbidden classes closed. Closeout “Next consumer: `runOptionGuardTag_eq` then packed `checkedBits`/`accepted`/`tree`/`bits`” is remaining work, not a compiled claim of this increment.
- There is no public `runOptionGuardTag_eq`. The compiled packing-contract is decode-none / empty tape only. Length-mismatch `[]` is definitional from `lenEqFlag` against private `trialsPrecisionRuler`, not a named public identity with semantic `x.trials * x.precision`. Accepting packed membership does not credit that missing agreement lemma.
- Private `nuPack` / `nuStep` / `nuRunEnc` / `natUnaryEnc` / `precisionUnary` / `trialsUnary` / `trialsPrecisionRuler` and their `_mem_FP` / `NuReach` lemmas are the unary nat-tree walker used by the public length flag. They are not additional public claims, not `runOptionTag_mem_FP`, and not a compiled theorem that the walker equals semantic `x.precision` / `x.trials`.
- Parent private `splitNode` / `nodeLeft` / `nodeRight` / `instEnc` / `precisionEnc` / `trialsEnc` are now used by that walker. They remain private scaffolding, not Checks-driven public theorems, and not `selectedPairedRun_mem_FP`.
- Semantic `runOption` still also requires `coins.length = x.trials * x.precision` and then `checkedBits`. This increment packs decode-success plus a packed length comparison against `trialsPrecisionRuler`. That is the freeze-permitted stop-loss, not a packing of the executor.
- The empty-tape Checks examples remain runtime packing identities `selectedPairedRun L (1/4) [] = []`, `paddedRunGuardTag eps [] = []`, and `runOptionGuardTag [] = []`. They are not a named public theorem that `selectedPairedRun ∈ FP`.
- `lean/README.md` and `lean/PROVENANCE.md` still list `decodeInputTag_mem_FP` among what is not claimed / remaining work. That catalog is stale relative to prior packed theorems and is conservative, not a claims expansion. This increment does not edit those files and does not convert the packed guard into manuscript Theorem 1.
- Unused `open`/`import` of `ActualSatToThreeSatSource`, `ActualHeadlineParameters`, and `RandomizedReduction` does not define a 3SAT→CMMSA map and does not inhabit `hSrcCmmsa`. `constFn_mem_FP []` is the fail arm of the packed tag; `id_mem_FP` is Cobham combinator API, not `fpSeededMap` of `id` or a dummy 3SAT identity.
- Certification was local target-fresh of frozen `c78bf1a` blobs after GCP `quantyra-lean-builder-01` remained TERMINATED (idle-stopped after the previous paddedRunGuard start; manuscript-repo content-addressed cloud gate not ported). The closeout records that limit. This lens does not treat local-vs-GCP as a claims expansion.
- Stopping after packed `runOptionGuardTag_mem_FP` / `runOptionGuardTag_none` / `runOptionGuardTag_empty` is the freeze-permitted stop-loss. It does not expand the claim boundary to `runOptionTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, or the rest of the SeededMap route.

Forbidden claims absent (`selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, dummy 3SAT→CMMSA identity, unconditional Theorem 1, Corollary 2, P vs NP, publication/DOI). Next: `runOptionGuardTag_eq` then packed `checkedBits` / `accepted` / output `tree` / `bits`, then `runOptionTag_mem_FP` and `selectedPairedRun_mem_FP`, then `selectedSeededMap`. Then a 3SAT (or 3-Lin) → `encodeInput` table compiler ∈ FP with `MapReducesVia` into a selected pipeline promise. Do not inhabit `hSrcCmmsa` with identity, and do not skip to Theorem 1.
