# Actual paddedRunGuard packed FP tag proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `b8ba7b55b542759d151410667ed56e50f6dcad3e` (`prove actual paddedRunGuard packed FP tag`). Parent `88cee568897b7264ec1510a2c5c7b25c9fd68da9` (`review actual decodeInput packed FP tag`; certified `decodeInputTag_mem_FP` at `f7f338b` / `29a8713`). Main SHA-256 `1B3BCB0452CD11DCB9C7B61E97F35FF8807297C0526F92534BF9DD2F3D7E69C9`, Checks `94D888D4645D8BD0EB4231F2766F8878030CDC3C585AC06B4F3A0B055E3344A8`. Independent `git cat-file` SHA-256 of both Lean blobs at `b8ba7b5` match the freeze and closeout (`blob de09577c74a2910aa2b81a360a5b4c3b5272878f` / `3f0fdc959eb9b333b79c142f4adc3a5cd3abd8c5`, 14990 / 1759 bytes, LF-only, 347 / 47 newlines, zero CR). Recomputed git-ids of those extracted blobs reproduce the same ids. Certification `cba404e1307030065f481ee119ef0f4edb16e1e1` (`certify actual paddedRunGuard packed FP tag (local after GCP idle-stop)`) adds evidence only; Lean blobs unchanged from `b8ba7b5` (`git diff --name-only b8ba7b5 cba404e -- '*.lean'` empty; same blobs at `HEAD`). GCP `quantyra-lean-builder-01` started then idle-stopped (manuscript-repo content-addressed cloud gate not ported); local target-fresh of frozen blobs twice, main+Checks exit 0, objects `85487B040ABB6C47267693905D152392E9F461705078D09FB665F903FCC58F5B` / `FC2513F4FBD85CC8AEDBD10BA4CB5E53B51034A6C387A912D7DEDEDD0573D5D3`, `MATCH_MAIN=True MATCH_CHECKS=True`. Printed axioms of `paddedRunGuardTag_mem_FP`, `paddedRunGuardTag_eq`, `paddedRunGuardTag_empty`, and `selectedCoinRuler_mem_FP` only `propext`, `Classical.choice`, `Quot.sound`. `ActualDecodeInputFP.lean` untouched (`blob aba9e488124928fba7d18fbb22f6d056b20c8eff`, SHA-256 `D2405DC2CE759190671801AEF669297C1F8DD9F56BAC1079F7E95D9EB87DDD87`, same as `88cee56` / `f7f338b`). Working tree is clean versus `HEAD` / `b8ba7b5` on `*.lean`. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze stop-loss: pack the decode + `coinRuler` length guard of `paddedRunOption`, prove `paddedRunGuardTag ∈ FP` and `paddedRunGuardTag_eq`, then stop. Public surface that compiled: `selectedPairedRun` (def), `selectedCoinRuler` / `_mem_FP` (parent), `paddedRunGuardTag` (def), `paddedRunGuardTag_mem_FP`, `paddedRunGuardTag_eq`, `paddedRunGuardTag_empty`. `selectedPairedRun_mem_FP` is omitted; `selectedSeededMap` is not defined. Parent `88cee56` left `selectedCoinRuler_mem_FP` and an uncertified `selectedPairedRun` (`paddedRun` on `pairFst` / `pairSnd`). This commit imports `ActualDecodeInputFP` and adds the packed guard. Packed walk: `instTag = decodeInputTag ∘ pairFst`; fail-closed `selectHead (emptyFlag (instTag z)) []` on decode failure; inner `selectHead` of `lenEqFlag (pairSnd z) (selectedCoinRuler eps (pairFst z))` keeps `true :: pair (pairFst z) (pairSnd z)` on length match and `[]` otherwise. `paddedRunGuardTag_mem_FP` is `selectHeadFn_mem_FP` / `emptyFlagFn_mem_FP` of already-certified `decodeInputTag_mem_FP`, `selectedCoinRuler_mem_FP`, pairing-block `fst`/`snd`, `pairFn_mem_FP`, and `cons_mem_FP true`. `paddedRunGuardTag_eq` is `∀ eps z`, matching the two `paddedRunOption` conjuncts that this increment owns: `decodeInput (pairFst z) = none` yields `[]`; `some _` and `|pairSnd z| = coinRuler eps |pairFst z|` yields the tagged canonical pair; length mismatch yields `[]`. `paddedRunGuardTag_empty` is that equality on `[]` via `decodeInput_empty`. Checks `#check` / `#print axioms` the parent ruler plus the three new names, inhabit `paddedRunGuardTag eps ∈ FP`, and reduce `paddedRunGuardTag eps [] = []`.

Hunts (absent). `sorry` on packed decode+`coinRuler` guard FP: no `sorry` / `admit` / `native_decide` / new `axiom` in main or Checks at `b8ba7b5`; no `decide` in the new region. Claiming `selectedPairedRun_mem_FP` or un-packed `paddedRun ∈ FP`: the public theorem is `paddedRunGuardTag eps ∈ Complexity.FP`, the packed `List Bool → List Bool` tag, not `paddedRunOption` / `paddedRun` / `runOption`; Checks inhabit `paddedRunGuardTag eps ∈ Complexity.FP` and do not inhabit `selectedPairedRun ∈ FP`. Dummy identity: `constFn_mem_FP []` is the fail / length-mismatch arm of `paddedRunGuardTag`; `id_mem_FP` is the parent Cobham `mulLenFn` API of `selectedCoinRuler_mem_FP`, not `fpSeededMap` of `id`, `identitySeededMap`, or a 3SAT→CMMSA compiler. Vacuous `paddedRunGuardTag_eq`: the statement is `∀ z`, including the nonempty `some`-and-length-match success arm that returns `true :: pair instanceBits coins`; empty / decode-`none` / length-mismatch are fail arms, not a restriction that makes the equality trivial. The `some _` binder is honest: this tag does not inspect policy fields. Checks not driving the real theorems: Checks `#check` / `#print axioms` / inhabit `paddedRunGuardTag_mem_FP`. New axioms: none; printed set is the standard trio. Theorem 1 / `hSrcCmmsa`: not inhabited.

Notes: Checks coverage of `paddedRunGuardTag_eq` is the empty `[]` example only; there is no `rfl` on a concrete well-formed `decodeInput` success with matching `coinRuler` length. `paddedRunOption` still has three un-packed conjuncts after this tag (`precision` vs `SamplingGuarantee.precision`, `trials` vs `ComputableSampleCount.count`, and `trials * precision ≤ coins.length`) plus `runOption` / `checkedBits`; nonempty `paddedRunGuardTag` does not imply `paddedRunOption ≠ none`. Private `splitNode` / `nodeLeft` / `weightsEnc` / `rowsEnc` / `paramsEnc` / `precisionEnc` / `trialsEnc` / `weightsLenBits` / `rowsLenBits` duplicate decode-input tree splits and are unused by the public theorems; they are scaffolding toward those remaining policy-field guards, not a packed executor. `paddedRunGuardTag` is still defined by composing semantic `decodeInputTag` (already in `FP` by the parent increment) with Cobham `selectHead` / `lenEqFlag`; that is the freeze packing, not a proof that `Option Bits` `paddedRunOption` is itself a Cobham function. `open RandomizedReduction` / `ActualHeadlineParameters` / `ActualSatToThreeSatSource` remain unused leftovers from the intended full SeededMap increment. Main does not import `*Checks.lean`. Stopping after `paddedRunGuardTag_mem_FP` / `_eq` is the freeze-permitted stop-loss. Next consumer remains packing `runOption` / `checkedBits` then `selectedPairedRun_mem_FP` then `selectedSeededMap`; do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `selectedPairedRun_mem_FP`, `runOption` / `checkedBits` packing, `selectedSeededMap`, inhabited `hSrcCmmsa`, dummy 3SAT→CMMSA identity, unconditional Theorem 1, Corollary 2, P versus NP, un-packed `paddedRun ∈ FP`, publication.
