# Actual packed Tree.parse FP tag proof-adversarial review

Verdict: GO-WITH-NOTES

Frozen `52b57f29cd7d317bfa77a6d046f58ce3d44633a7` (`prove actual Tree.parse packed FP tag`). Main `563831C5485F44DE298A0E896BD6A9843F5B338F8CDA428F15DFBFB9EB057074`, Checks `B9DA8EB5A89BDDA4F81FFA25D2B9D869E20B65978AD3A7ED8C344A131EDDBCA3`. Certification `6cdf45bfa10fe10144b5e5b0c603d5a6740c1165`. Gate named `52b57f2` not `bba6dbb` (`certify.sh` rejects a sibling `bba6dbb` gate log; `gate.sh` pins `EXPECTED_HEAD=52b57f29…`, `EXPECTED_MAIN=563831C5…`, `EXPECTED_CHECKS=B9DA8EB5…`). 7 stages exit 0 (`exception-repair`, `formula`, `cmmsa-codec`, main×2, checks×2); objects `BEF82D9A5F18A1AB150C4945F370369654706539200555D47FD5437FE7D3F34F` / `3E17CACAAE1A4DF4AB37EF6FA62B53E167605BF8F0FA5F84E1ED5F5B26473D21`. Independent `git cat-file` SHA-256 of both Lean blobs at `52b57f2` match the freeze and the gate (`blob 701db566e3c5b2b9bede4b0940daeab88c64f779` / `eec2a3615324a2a939f5b7c5bab84513b7047911`, 33468 / 996 bytes). Cert commit `6cdf45b` adds evidence only; Lean blobs unchanged from `52b57f2` (same blob ids; `git diff --name-only 52b57f2 6cdf45b -- '*.lean'` empty). Working-tree SHA-256 of both Lean sources match. Independent rehash `result=PASS`, 61 rows 0 mismatches. Forbidden scan clean. Printed axioms of `treeParseTag_mem_FP` only `propext`, `Classical.choice`, `Quot.sound`; `treeParseTag_empty` / `treeParseTag_leaf` depend on none. I made no Lean-source or Git changes and ran no additional broad build.

This increment is the freeze stop-loss: packed `Tree.parse` in `FP`, then stop. Public surface that compiled: `treeParseTag` (def), `treeParseTag_empty`, `treeParseTag_none`, `treeParseTag_some`, `treeParseTag_leaf`, `treeParseTag_mem_FP`. `decodeInputTag` is omitted; `decodeInputTag_mem_FP` and `selectedPairedRun_mem_FP` are not declared; `selectedSeededMap` is not defined. `treeParseTag z` is the freeze packing of `CMMSACodec.Tree.parse (z.length + 1) z`: `none ↦ []`, `some (t, rest) ↦ true :: pair (encode t) rest`. That definition is total on every `List Bool`, including empty, truncated `true :: []`, leftover suffix, and fuel-zero-shaped malformed input (`parse 0 _ = none` is not the public fuel; public fuel is `|z|+1 ≥ 1`). `treeParseTag_empty` / `treeParseTag_leaf` are `rfl` against `parse 1 [] = none` and `parse 2 [false] = some (leaf, [])` with `encode leaf = [false]`. `treeParseTag_none` / `treeParseTag_some` are the two match arms. `treeParseTag_mem_FP` is Cobham `iterate_mem_FP` of a total bitstring step `parseStep`, init `pair z (pair parseMode [])`, ruler length `3|z|+3`, width `16(|z|+1)`, then `packTag`, then `mem_FP_of_eq` along `treeParseTag_eq_iter`. The equality is `∀ z, treeParseTag z = packTag (parseStep^[(parseRuler z).length] (initState z))`, not a restriction to `parse_encode` well-formed trees. Semantic interpreter `semStep` preserves `evalState`, `evalState (initSem z) = Tree.parse (|z|+1) z`, stuckness is reached by step `measure+1` with `measure (initSem z) = 3|z|`, and `pack_eval` on a stuck state emits `[]` on fail and `true :: pair (encode t) remaining` on success (rest is `stRem`, not dropped). `parse_consumed` is the converse of `parse_encode` (success implies `bs = encode t ++ rest`); it is used to justify fuel-shrink on suffixes, not as a substitute for malformed coverage. Checks `#check` / `#print axioms` the six public names, reduce `treeParseTag [] = []`, `treeParseTag [false]` against `treeParseTag_leaf`, and truncated `treeParseTag [true] = []` by `rfl`, and inhabit `treeParseTag ∈ FP`.

Hunts (absent). `sorry` on parse FP: no `sorry` / `admit` / `native_decide` / new `axiom` in main or Checks. Dummy identity: `id_mem_FP` appears only as the stay-put branch of `parseStep` on fail/success (`selectHead` returns `st`); it is not `fpSeededMap` of `id`, not `identitySeededMap`, and not a 3SAT→CMMSA compiler. Claiming `Tree.parse ∈ FP` from `parse_encode` only: `parse_encode` is not referenced; `treeParseTag_eq_iter` plus `evalState_iterate` / `evalState_init` agree with `Tree.parse (|z|+1) z` on every `z`. `treeParseTag` not total on malformed: the def is a total `List Bool → List Bool`; empty, truncated `true`, and fail-mode pack all map to `[]`. Iterate not agreeing on every `z`: the ruler is `3|z|+3 ≥ measure+1`; `reach_iterate` bounds width on every iterate, not only well-formed ones. Unpacking that drops rest: success pack is `true :: pair (stPayload st) (stRem st)`; `treeParseTag_some` keeps `rest` inside `Complexity.pair`. New axioms: none; printed set is the standard trio.

Notes: Checks coverage of malformed is `[]` and `[true]` only; leftover-rest packing is the statement of `treeParseTag_some` / `pack_eval` success, not an extra `example`. Nested `true :: false :: false` is not in Checks; the step/eval invariant covers it. `id_mem_FP` is the Cobham stay-put API, not a dummy reduction. Unused `simp` arguments warn on main compile (lines 103, 117, 422) and do not change the theorems. Main does not import `*Checks.lean`. Stopping after `treeParseTag_mem_FP` is the freeze-permitted stop-loss. Next consumer remains `decodeInputTag_mem_FP` (readInput packing after trailing-empty tests on `treeParseTag`), then `selectedPairedRun_mem_FP`; do not sorry those, and do not inhabit `hSrcCmmsa` with identity.

No HIGH vacuity, hypothesis, or statement bug. Not credited: `decodeInputTag_mem_FP`, `selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, P versus NP, publication.
