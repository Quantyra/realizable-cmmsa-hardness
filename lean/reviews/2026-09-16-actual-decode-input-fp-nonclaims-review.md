# Actual packed decodeInputTag FP tag: non-claims-boundary review

**GO-WITH-NOTES.**

Frozen source `f7f338bdd28101ff66fa482716a435ca783cd191` (`prove actual decodeInput packed FP tag`). Gate/closeout name `f7f338b`. Main `ActualDecodeInputFP.lean` SHA-256 `D2405DC2CE759190671801AEF669297C1F8DD9F56BAC1079F7E95D9EB87DDD87` (git blob `aba9e488124928fba7d18fbb22f6d056b20c8eff`, 402609 bytes, LF, git-object SHA-256 `B051005536E0780E24D2E85C026E45867EC9625851F9D28A319EA389D2F7AC3D`). Checks SHA-256 `BAD35DD0BFFD06A007E5C552C494AAA823AF1B266CB2F74498EF01E0B614DC9D` (git blob `aef903bfaf40998fb168184274973214f62bcee4`, 6581 bytes, LF, git-object SHA-256 `B3CC116C69C80931EF582D394E33D492AD94F5AA7262AF851FE899650D39612F`). Evidence `lean/evidence/2026-09-16-actual-decode-input-fp-fresh-run/` and closeout `lean/evidence/2026-09-16-actual-decode-input-fp-closeout.md`. Independently `git cat-file`’d and rehashed those blobs at `f7f338b`; certify commit `29a8713`, `HEAD` (Lean byte-identical), and the working tree match the freeze and the closeout table. Certification commit `29a8713` is evidence-only and did not change the Lean sources. Recorded oleans `773E14769F846688877E9E0857873E8AB3286E4CBA43D3D3161D6A717ECBB8E8` / `BB977DD6D036454A4C9B5978347A5D345D62F3CA0B816030172A55B57DDF3BC2` (local target-fresh twice after GCP reauth failure, matching hashes, exit 0). Closeout SHA-256 `7758033C632326D61CB27C3912AF3E54059D4341BDFEF38B50D061366BE9DC14` (git blob `f4596e4e40da06ad4fcc2ec37e152452e51fc38e`). Local cert log git-blob SHA-256 `5B1FEBBA32857A4E40675875E74C551A2FF553A6CCECF715EA3BB657C752F677` (blob `48b66019688b05706d1b41decf070ebb2e54e0d5`, 9592 bytes, LF). GCP unavailability note SHA-256 `7C78C782714143BE1A8E7120ED80A419BA48F11CACAD0EB5486C1F92B9885949`. Parent Lean blob `8ebb66f8f643bd3cd74c9c828c6d88aa728b33e9` (`98ffac0` / `a591714`). `MANUSCRIPT.md` unpublished bytes are unchanged (`9ec48580b55b3e34afad115199817da3c1b06eb3`, SHA-256 `FF00997C8C243E88982C41B0B0E24FAAAAFE36CEC6F1903824599DC242538686`). `ActualSelectedCmmsaSeededMap.lean` is untouched (`1b47db7`, blob `9fa1a0ed2649fcb3424c62a861abd545f24b0357`). This lens did not compile or edit Lean. Review is of git blobs at `f7f338b` only.

Accepted, freeze stop-loss only (packed `readInputTag ∈ Complexity.FP` with tree agreement, and packed `decodeInputTag ∈ Complexity.FP`; no un-packed `decodeInput ∈ FP` as Theorem 1 material):

- `readInputTag z` is a total `List Bool → List Bool` packing of `ExecutablePipelineInput.readInput` by nested `selectHead` / `emptyFlag` over `splitNode` shape, weights (`readListTag`), packed rows/`ValidRows` (`readRowListTag` / `readTableTag`), parameters, precision, and trials, then `wrapInputEnc`. Empty / malformed / validation failure go to `[]`. Nonempty success is `true :: encodeInput x`. `readInputTag_mem_FP` places that packed function in `Complexity.FP` from Cobham composition of the parent packed readers and `listLenBits` / `readNatTag`. It is not a semantic identity `readInputTag = pack ∘ readInput` on malformed tapes outside `encode t`, and it is not un-packed `readInput ∈ FP`.
- `readInputTag_of_tree` is the freeze tree-agreement: `readInputTag (encode t)` equals `[]` when `readInput t = none` and `true :: encodeInput x` when `readInput t = some x`. It is agreement on a complete tree encoding, not `decodeInput ∈ FP`.
- `decodeInputTag_mem_FP` places the already-public packing `decodeInputTag` in `Complexity.FP` by private `decodeInputTag_mem_FP_of_read` after `readInputTag_mem_FP`, using private `decodeInputTag_eq_guard` (`treeParseTag` plus `readInputTag`, reject trailing bits). It is packed `decodeInputTag ∈ Complexity.FP`. It is not `decodeInput ∈ FP`, not `selectedPairedRun_mem_FP`, and not Theorem 1. `decodeInputTag_none` / `_some` remain the packing-contract lemmas from prior increments.
- Parent packed memberships (`gcdBits`, `readRatTag`, `readSignedTag`, `readListTag`, `readFormulaTag`, `readRowTag`, `readRowListTag`, `readParametersTag`, `readTableTag`, `listLenBits`, and their `_of_tree` / `_of_pair` / `_of_rows` facts) remain prior certified packed theorems. They are not un-packed semantic `decodeInput ∈ FP`.
- `selectedSeededMap` is not defined. `hSrcCmmsa` is not inhabited. There is no dummy 3SAT→CMMSA identity. `ActualSelectedCmmsaSeededMap.lean` is untouched and still omits `selectedPairedRun_mem_FP`.

The module header denies `selectedSeededMap`, inhabited `hSrcCmmsa`, unconditional Theorem 1, Corollary 2, and P vs NP. Checks `#check` / `#print axioms` `decodeInputTag`, `decodeInputTag_empty`, `decodeInputTag_none`, `decodeInputTag_some`, `gcdBits`, `gcdBits_mem_FP`, `gcdBits_odd_pair`, `readRatTag`, `readRatTag_mem_FP`, `readRatTag_of_tree`, `readSignedTag`, `readSignedTag_mem_FP`, `readSignedTag_of_tree`, `readListTag`, `readListTag_mem_FP`, `readListTag_of_tree`, `readFormulaTag`, `readFormulaTag_mem_FP`, `readFormulaTag_of_pair`, `readRowTag`, `readRowTag_mem_FP`, `readRowTag_of_pair`, `readRowListTag`, `readRowListTag_mem_FP`, `readRowListTag_of_pair`, `readParametersTag`, `readParametersTag_mem_FP`, `readParametersTag_of_tree`, `readTableTag`, `readTableTag_mem_FP`, `readTableTag_empty`, `readTableTag_of_rows`, `listLenBits`, `listLenBits_mem_FP`, `listLenBits_of_listTree`, `listLenBits_leaf`, `readInputTag`, `readInputTag_mem_FP`, `readInputTag_of_tree`, and `decodeInputTag_mem_FP`; inhabit `gcdBits ∈ Complexity.FP`, `readRatTag ∈ Complexity.FP`, `readSignedTag ∈ Complexity.FP`, `readListTag ∈ Complexity.FP`, `readFormulaTag ∈ Complexity.FP`, `readRowTag ∈ Complexity.FP`, `readRowListTag ∈ Complexity.FP`, `readParametersTag ∈ Complexity.FP`, `readTableTag ∈ Complexity.FP`, `listLenBits ∈ Complexity.FP`, `readInputTag ∈ Complexity.FP`, and `decodeInputTag ∈ Complexity.FP` via `example : decodeInputTag ∈ Complexity.FP := decodeInputTag_mem_FP`; reduce the leaf `_of_tree` / `_of_pair` examples, including `readInputTag (encode leaf) = []`; and reduce `decodeInputTag [] = []` and truncated `decodeInputTag [true] = []`. Checks do **not** `#check` `selectedPairedRun_mem_FP`, `selectedSeededMap`, or `hSrcCmmsa`, and do **not** inhabit un-packed `decodeInput ∈ Complexity.FP`. Main does not import `*Checks.lean`. `#print axioms readInputTag_mem_FP` / `readInputTag_of_tree` / `decodeInputTag_mem_FP` are `propext`, `Classical.choice`, `Quot.sound` only. Forbidden-scan of the freeze blobs is clean (`sorry` / `admit` / `native_decide` / new `axiom` absent; Theorem 1 / Corollary 2 / P vs NP / `selectedSeededMap` / `hSrcCmmsa` appear only in denials). README, `CITATION.cff`, `lean/README.md`, `lean/PROVENANCE.md`, and `MANUSCRIPT.md` are unchanged by this increment.

Notes, not blocking:

- The Main module docstring still frames packed helpers *toward* `ExecutablePipelineInput.decodeInput` and now says `decodeInputTag_mem_FP` follows `decodeInputTag_mem_FP_of_read`. That is the file-goal catalog line plus this increment’s packed membership, not a theorem that un-packed `decodeInput ∈ FP` is Theorem 1 material.
- The `readInputTag` docstring says pack `readInput` on a complete tree encoding, with empty = none and nonempty = `true :: encodeInput x`. That is the packing-contract line. `readInputTag_of_tree` is only the `encode t` case.
- Module/commit titles that say “actual decodeInput packed FP tag” / `ActualDecodeInputFP` / Checks “packed decodeInput FP tag” are catalog labels. They name packed `decodeInputTag ∈ Complexity.FP`, not un-packed `decodeInput ∈ FP`, not `selectedSeededMap`, not inhabited `hSrcCmmsa`, not unconditional Theorem 1, Corollary 2, publication, or `P` versus `NP`.
- Closeout phrasing “packed decodeInput in Cobham FP” is `decodeInputTag_mem_FP` via `readInputTag_mem_FP` / `readInputTag_of_tree`. Closeout “Not `selectedPairedRun_mem_FP`. Not `selectedSeededMap`. Not Theorem 1.” keeps the forbidden classes closed.
- Private `wrapInputEnc` / `wrapInputEnc_eq` / `inputWTag` / `inputRArg` / `inputRTag` / `inputTTag` / `inputPArg` / `inputPTag` / `inputBArg` / `inputBTag` / `inputMArg` / `inputMTag` / `inputWrap` / `*_mem_FP` / `decodeInputTag_eq_guard` / `decodeInputTag_mem_FP_of_read` support the public packing and membership. They are not additional public claims and are not `selectedPairedRun_mem_FP`.
- The empty-tape and truncated-`true` checks remain runtime packing identities `decodeInputTag [] = []` and `decodeInputTag [true] = []`. They are not a named public theorem that `decodeInput ∈ FP`.
- `lean/README.md` and `lean/PROVENANCE.md` still list `decodeInputTag_mem_FP` among what is not claimed / remaining work. That catalog is stale relative to this packed theorem and is conservative, not a claims expansion. This increment does not edit those files and does not convert the packed tag into manuscript Theorem 1.
- `ActualSelectedCmmsaSeededMap.lean` still omits `selectedPairedRun_mem_FP` because semantic `paddedRun`, `decodeInput`, and `Tree.parse` are not on the Cobham `List Bool → List Bool` surface. Packed `decodeInputTag_mem_FP` does not inhabit `selectedSeededMap` or `hSrcCmmsa`.
- Certification was local target-fresh of frozen `f7f338b` blobs after GCP `quantyra-lean-builder-01` reauthentication failure. The closeout records that limit. This lens does not treat local-vs-GCP as a claims expansion.
- Stopping after packed `readInputTag_mem_FP` / `readInputTag_of_tree` / `decodeInputTag_mem_FP` is the freeze-permitted stop-loss. It does not expand the claim boundary to `selectedPairedRun_mem_FP`, `selectedSeededMap`, or the rest of the SeededMap route.

Forbidden claims absent (un-packed `decodeInput ∈ FP` as Theorem 1 material, `selectedPairedRun_mem_FP`, `selectedSeededMap`, inhabited `hSrcCmmsa`, dummy 3SAT→CMMSA identity, unconditional Theorem 1, Corollary 2, P vs NP, publication/DOI). Next: `selectedPairedRun_mem_FP` then `selectedSeededMap`. Then a 3SAT (or 3-Lin) → `encodeInput` table compiler ∈ FP with `MapReducesVia` into a selected pipeline promise. Do not inhabit `hSrcCmmsa` with identity, and do not skip to Theorem 1.
