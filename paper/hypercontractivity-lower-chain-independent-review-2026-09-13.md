# Independent manuscript integration review: lower hypercontractivity chain

2026-09-13. S3128/S3137. Reviewer: incidence_complexity_review. Verdict: GO for the exact lower-chain integration through (A12), with the scope and contribution disclosures below. No blocking missing proof or transcription defect was found. This is not Lean certification, independent human peer review, publication approval or a novelty finding.

## Exact candidate and reviewer scope

* `paper/submission-manuscript.md`: SHA256 `d8b03b5fa1524391bc8f492086de8859ba25b361deb2bb86815a1dfbff33f981`.
* `paper/body.tex`: SHA256 `766ebbd2545545063ac0290c68264436b4ef09545289c6986ff5feeb93b80dc7`.
* `paper/correspondence.json`: SHA256 `4dcedadece4b46ef03246c452a0dc6be9544e755df69677f1a56bae94f3dcc88`.
* Unchanged renderer: SHA256 `85eb3770b711843212764ee9785427c3c3ec2454da8a7c0d33b3a2662ae5e8d4`, raw-equal to repository HEAD.

I read the complete canonical appendix from its character definitions through (A12), source lines1342-1806, and inspected the complete source/generated-body diff against the previously frozen paper. Compact separately reviews the upper conversion/dyadic/application chain. I did not author this manuscript insertion. I authored several underlying DR6/counting/assembly derivations and reviewed transfer/W6; this is an independent integration check against their separately reviewed accepted records, not a claim that I independently certified my own mathematical contributions. The accepted records are in formal-pvnp, including core0e5e5d0, counting ece5d79, DR6/W6 c7aaf61 and fourth-moment joint archive5f203d3.

## Checked proof dependencies

Lines1381-1469 give normalized characters, affine domains, actual ordinary/hybrid/rank-poset selectors, rank lowering with an explicit below-order zero case, and nested composition including its canonical phase. Averaged energy (A2) retains the T average; the text explicitly denies fixed-base Parseval where frequencies coalesce. The rank-poset block fiber(A3) uses two independently determined off-diagonal maps and the forced remaining block, preserving its exact2^(2kl) multiplicity. No contraction is inferred for D_X.

Lines1471-1504 derive W6 from coherent fibers and selected Fourier energy a_X<=E. The predecessor projection count, Gaussian bound, exponent-6k^2 and67/63 constant are charged. The finite GL4 failure is limited to the pinned weight4 auxiliary statement; it does not claim the target theorem false. The zero-degree term is handled separately.

Lines1506-1608 include both actual convolution classes. Their cover is proved by the kernel construction, with the explicit kernel-generator images needed for rank-additive decompositions. The possible overlap is handled by partition into F2 and its complement; absolute Fourier coefficients majorize only the first class. Its degree-two Boolean encoding distinguishes the two copies of the zero-map variable, proves the81 estimate by sign induction, and handles ordered-pair and empty-character collisions in the squared encoding. The F2 Mobius inversion retains the complex coefficients and their cancellations; weighted counting uses the actual rank-X bound2d. The resulting coefficient is6d^2, not the stronger printed3d^2. Both class estimates and their assembly are included, not merely cited.

Lines1610-1717 prove T1/T2 rather than assuming an appendix identity. T1 includes both selector directions, uniqueness and the theta bijection. T2 includes the reverse image/agreement argument establishing X<=Y before writing Z=Y-X, then maximal restriction/quotient rank loss, the unique complement C and the dimension argument fixing H. The forward decomposition attains the two image components on ker Z and B2. Both phases and final frequencies are explicit. These are actual character identities; no fixed-base orthogonality premise replaces them.

Lines1719-1793 use simultaneous strict lower-degree induction on all finite spaces. They retain the corrected ordinary cutoff d+k and the true mixed cutoff d, complement Holder cost, unique intermediate-pair reconstruction, and original-T translation/Fubini with no cardinality factor. The final multiplicity(A9) has a graph choice for image and kernel and a unique X once Y is fixed; no additional GL factor is assumed. Exponent(A10) and the two geometric-series cases give the stated57-pre(A11). W6 absorbs the residual weight via the explicit-29dk slack, preserving100. The zero-order term supplies ||f||_2^4, not the target fourth moment, so this does not make the induction circular.

Lines1795-1806 then count actual hybrid pairs at each rank j by the Gaussian quotient formula, bound the multiplicity by2^(3j^2), and use averaged energy to obtain103. The case j=0 has multiplicity1; higher-order derivatives vanish. This closes the stated lower chain with no remaining named analytic transfer premise inside it.

The dimensional and base cases used by this chain are present: finite spaces may be zero-dimensional; d=0 is separate; the below-order branch precedes degree subtraction; unavailable subspaces/frequencies contribute zero; all shifts are genuine full-space bases or canonical embeddings. Standard finite linear algebra, Parseval, Holder and finite sums are used as mathematics, not represented as Lean theorems.

## Diff and deterministic transcription

The complete canonical diff contains exactly three local inverse-proof reference/status replacements and the appended proof block. The inverse constants, spectral estimates, complement/robust proofs, parameter order and final learning statements were not altered. The body diff has the matching three replacements and appendix insertion. The correspondence changes reflect line shifts and the additional raw block; all130 source-span hashes independently match, and the existing numbered-display count remains30.

The first three raw LaTeX blocks are literal in the body. The fourth is exact after the existing renderer line262 Gaussian transformation from `{a\brack b}_2` to `\genfrac{[}{]}{0pt}{}{a}{b}_2`. An initial query about this difference was resolved by inspecting the unchanged renderer; there is no hand-edit finding or pending correction. All four blocks match under that precise transformation.

For deterministic verification I executed the unchanged renderer with `Path.write_text` replaced by a read-only comparison function. Both would-be generated byte strings exactly matched the current body and correspondence files; no output file was written. The replay reported113222 body characters,30 displays and130 source spans. The three candidate raw hashes were rechecked after this read-only verification.

## Boundary and next stage

The appendix attributes the method to the precise Ellis and Evra source versions and states ordinary mathematical derivation, not Lean or novelty. Other upstream PCP, compilation and learning contracts remain imported. This GO is for the lower-chain integration at the exact hashes above; upper-chain/application review and root consolidation remain separate. No PDF build or visual QA was performed, and the existing PDF is not certified against this new source by this note. No compiler, source/generated-file edit, Git or public action occurred during review; only this review document was written.
